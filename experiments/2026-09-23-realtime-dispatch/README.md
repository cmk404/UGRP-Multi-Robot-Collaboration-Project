# 로컬 MuJoCo 실시간 실행 검증

물리 owner가 RGB 렌더·판단·관찰 창 때문에 멈추는 문제를 분리한다. `protocol.json`의 동일 open/seed11 계획·모델·카메라·접촉 설정을 고정한다. 실제 timestep은 0.00025초, weld는 OFF, 새 LLM 호출과 학습은 0회다. 각 물리 실행 전 코드를 커밋하며 원본을 덮어쓰지 않는다.

첫 계측과 실패 실행의 해시·소스·입력·영상·TensorBoard 검증은 `initial-diagnostics.json`에 있다. 180초 cProfile 진단은 기한 중단이며 완주 시간 비교에 포함하지 않는다. v8(`f2d6ced`)은 95.803초 뒤 공동 운반에서 중단됐다. 5개 판단 모두 실제 RGB 촬영 시각보다 0.70–0.74 SIM초 늦어 0.6초 제한을 넘었고, 운반 명령은 양쪽 HOLD였다. 낮은 종료 시간을 속도 개선으로 해석하지 않는다.

원본은 기본 checkout의 `outputs/simulation-realtime-20260923/{profile-baseline,native-v8}`에 보존한다. 기존 TensorBoard 서버의 `0923-실시간-진단/{계측,v8}`에 두 실패를 별도 등록했으며 이벤트 값 로딩과 MP4 Range 206 응답을 검증했다. 새 후보는 접근·파지의 비동기 관측, 동일 JPEG 분할 재사용, 지연 원인 분리 및 독립 관찰 창의 Mac launcher를 통합한다. 완주 결과는 별도 결과 기록으로 추가한다.

v9(`5f14753`)의 native 실행에서는 motion 구간 56.480 SIM초/56.482 wall초(1.000×)를 기록했다. 모든 pair 영상은 0.6초 제한 이내였지만 초기 접근 120회 한도를 소진했고 화물은 이동하지 않았다. `independent-audit-v9.json`과 `v9-pair-stage-latency-audit.json`에 원본 해시 및 수치를 기록한다. 0.2초 명령마다 관측 중 중앙값 0.26초의 명령 공백이 있었다. v10은 다음 RGB 처리를 이동 명령과 겹치고 정지 확인의 전체 dwell을 유지하는 후보다. 기존 성공 v7과의 완주 시간 비교는 성공 재실행이 생긴 뒤에만 계산한다.

단위 검사·동일 픽셀/분할 후보 검사는 실제 운반 성공을 의미하지 않는다. headless·native·녹화와 공동 운반·ACT·새 지도 성공을 각각 구분한다.

v10(`25a88b8`)은 motion 28.980 SIM초/28.984 wall초(1.000×), 36.962 wall초에 접근 120회 한도를 소진했다. pair RGB age 중앙값 0.20초·최대 0.40초로 신선했지만 명령 주기 중앙값 0.22초에 0.20초 lease가 끝나 반복 정지가 남았다. 양측 120회 동일 시각 발행이며 각 로봇 명령 공백 합계 4.86 SIM초였다. v11은 초기 접근 이동 lease만 0.25초로 조정하는 단일 변수 진단이다. 모델·판단 임계값·120회 한도·0.6초 영상 TTL·정지 확인 dwell은 유지한다.

v11(`ea9759e`)은 coarse 71회 및 정밀 yaw/lateral/yaw를 통과했으나 정밀 forward 단계의 이동 160회 한도를 소진했다. 모델 입력 유효성 이탈이 아니라 이동 예산 실패다. wall107.137초, motion98.310 SIM초/98.821 wall초; 물리·프로토콜 성공 false. v12는 open realtime 정밀 접근의 이동에도 같은 최대0.25초 갱신을 적용한다. 물리 감쇠와 성공/지원 임계값은 변경하지 않는다. `v10-drive-kernel-trajectory-audit.json`의 단일 로봇 무하중 진단은 동일 명령 trace에서 kernel/legacy의 모든 timestep 상태 차이0과 정지 공백의 감쇠 효과를 구분해 기록한다.

v12(`6fd23ae`)는 coarse88회, 정밀 7단계의 정지 확인, 파지를 모두 통과하고 계획된 운반 10회 한도에서 중단됐다. wall109.115초, motion100.220 SIM초/100.232 wall초. 운반 영상 age0.44–0.64초, 유효9/오래된영상HOLD1이며 물리·프로토콜 성공은 false다. 운반 첫 프레임의 빔 특징 계산과 명령 갱신 공백을 추가로 검토해 `protocol.json`에 유한한 후속 진단 예산을 기록했다. 완료하지 않은 진단 시간을 완주 속도 비교로 사용하지 않는다.

v13의 첫 프레임 빔 특징 계산은 단면별 반복 배열 탐색을 그룹 정렬로 바꾸고, contour의 경계 상자 안에서 마스크를 만든 뒤 원래 픽셀 좌표를 복원한다. v12 저장 TOP 11장의 18개 포화도 조건별 전체 후보는 기존 결과와 정확히 일치했다. 첫 계산 중앙값은 약0.147→0.044 wall초였다. 이 CPU 재생 수치를 전체 시뮬레이션 속도로 해석하지 않으며, 운반 갱신과 결합한 실제 진단을 별도로 수행한다.


v13(`d4b2059`) 진단은 운반 40회 제한까지 진행했고 motion105.140 SIM초/105.402 wall초, stale0이었다. 같은 소스 전체 실행은 운반167회 뒤 현재 RGB의 고포화 중심선 합의 부족으로 중단됐다. wall167.796초, 빔 이동0.548m, box이동0.109m, 물리·프로토콜 성공 false이며 완주 시간 비교에서 제외한다. `independent-audit-v13*.json`에 해시와 TensorBoard 실패 등록을 보존한다.

v14는 기존 고포화 판정이 실패할 때만 현재 저포화 RGB의 동일 중심선 합의와 고포화 fit의 교차 확인을 요구한다. 원본 v13/v7 유효판정277개는 그대로이고 실패9개가 재생에서 회복됐으나 새 조건 성공률은 아니다. Solo 작업자의 권한 요청은 사본에서 계산하고 owner가 최신 결과의 권한을 재검증해 확정하며, 직접 carry/yield 이동은 최대0.25초 명령과 다음 관측을 겹친다. 원본영상+0.6초 만료와 zero/HOLD·pose 간격은 보존한다. 추가 유한 검증 예산은 protocol에 기록하며 실제 완주는 별도 확인한다.


v14(`427d6ad`)는 motion216.318 SIM초/217.362 wall초(0.9952배속), wall226.106초에 상자 own-RGB 부착 판정 실패로 종료됐다. 빔1.342m·상자1.057m 이동, strict 적재 동시이동11.8 SIM초지만 두 화물 완료는 false다. 공용 apron 대기115회 동안 candidate 전체를 폐기해 마지막 채택 own 영상과 다음 영상 간격이42.023 SIM초 벌어졌다. 실제영상·사후referee 대조는 상자가 여전히 로봇과 함께 유지된 정황을 보여 주며, 판정 임계값을 낮추는 대신 대기 중 관측 상태 갱신 경계를 수정한다.

동일 RGB x구간을 비교한 v13/v14 평균진행은 약1.23/1.62cm per SIM초(고정9cm 투영면 환산)였다. v14 명령갱신 중앙값0.36초와0.25초 lease 사이에 중앙0.12초 공백이 남았다. v7의 약8.07cm per SIM초와 같은 임무전체속도 비교가 아니며, real-time 시계만 복구해서는 이 반복감속을 없애지 못했다. 다음 후보는 현재 RGB 분석 중 다음 coherent snapshot 하나를 미리 처리해 갱신 간격을 줄인다. 원본영상TTL0.6초·최대lease0.25초·순차tracker commit은 유지한다.


v15(`efe19d0`)는 두 화물의 물리 배치·방출 성공을 기록했지만, solo 300 SIM초 한도로 마지막 정리 절차가 중단됐다. wall309.681초, protocol_complete=false이며 성공 완주 속도 비교에는 사용하지 않는다. motion300.160 SIM초/301.432 wall초(0.9958배속), 적재 동시이동0.2 SIM초다. 다음 영상 미리 요청하기는 첫 구간을26.99→7.29 SIM초로 단축했지만 전체 RGB age 중앙값은0.32→0.44초로 늘어 최대0.25초 lease가 중앙0.16초로 잘렸다. 대기열에서 오래된 영상을 만든 영향과 자원 대기272회를 별도 수정한다. 원본 해시와 TensorBoard 값은 `independent-audit-v15-full.json`에 있다.

v16은 actor RGB만 렌더 작업 시작 시 락 아래 동일 순간을 복사한다. 요청 순서가 아닌 실제 복사 시점에 frame_id/SIM timestamp를 부여하고 원래0.6초 TTL을 유지한다. 영상 녹화는 기존 요청 시각 캡처를 보존한다. Solo의 명시적 자원 대기와 활동 시간을 분리하되 전체 wall600초 한도는 유지한다. 미리 늘린 완료 주장 없이 새 고정 실행에서 검증한다.


v16(`bb9d4ba`)은 wall255.344초, motion245.241 SIM초/246.306 wall초(0.9957배속)에 solo 판단600회 제한으로 종료됐다. 빔만 물리 성공, 상자·프로토콜 실패다. RGB age 중앙값은0.317초로 줄었지만 pair 갱신0.320초와 lease0.25초 사이에 중앙0.070초 공백이 남았다. 상자 활동227.513 SIM초·자원 대기17.729초이며 마지막 경유점에서 판단 한도를 소진했다. `independent-audit-v16-full.json`과 실패 TensorBoard snapshot에 남긴다.

관찰창의 자연어 패널은 실제 저장 LLM 전달12건 중 최근3건을 새 호출 없이 재생해 검증했다. 소스`5e25bd8`의 기본 state-only sync 창에서 한글·발신/수신·원문 두 줄·기록 재생0건 표기가 모두 보였다. `communication-native-default-primer-smoke.json`에 별도 UI 확인을 기록한다. 이 관찰 전용 검증은 운반 성공이나 새로운 LLM 대화 검증이 아니다.

v17 고정 소스 `817872651083143fe4ff46b11dba30915e34f5ff`, 실행 번들 SHA-256 `8a686939f6505bfaae2f4ebadb3e637b0e229b63e25dfb96877c411235bf6125`에서 동일 open/seed11 저장 계획과 입력으로 native 전체 실행 2회를 마쳤다. 두 번 모두 빔·상자의 물리 배치·방출과 프로토콜 종료가 성공했다(2/2). 첫 실행은 wall 312.371초/SIM 307.360초, 반복은 wall 213.656초/SIM 210.170초였다. 새 LLM 호출·학습·weld는 0이다. 원본 파일 해시, source/input 불변, 원본 RGB 시간 제한을 지킨 pair 갱신 명령, 사후 referee는 `independent-audit-v17-{full,repeat}.json`에 각각 보존한다. 기본 checkout의 원본 `outputs/simulation-realtime-20260923/native-v17-{full,repeat}`와 TensorBoard `0923-실시간-진단/v17-{full,repeat}`는 별도 스냅샷이다.

반복은 첫 성공의 solo 판단 598/600이라는 좁은 여유 때문에 `budget-review-after-v17-success.json`에서 600 wall초 이하 1회만 추가 승인한 유한 검증이다. 같은 소스·계획·입력인데도 pair 운반 판단은 357→121회, 첫 경유점 SIM 시간은 14.51→8.55초, 다음 긴 구간은 70.37→12.87초로 달랐다. 적재 동시 이동은 11.1→2.2 SIM초이며 두 실행 모두 물리 성공이다. 이는 시계만 빨라진 결과가 아니라 실제 RGB 판단·명령·물리 진행의 큰 변동을 포함한다. 최대 명령 0.25초와 영상 원본 시각+0.6초 제한은 보존했지만, 첫 실행의 solo 예산 여유 2회와 운반 명령 공백·주행 속도 변동은 남아 있다. 따라서 213.656초를 보장 완주 시간이나 일정한 주행 속도로 제시하지 않는다.

관찰창은 첫 실행 중 한글 상태 패널과 저장 계획 재생·새 자연어 메시지 0개 표시를 확인했다(`communication-native-v17-live-ui.json`). 이것은 별도 UI 근거다. 성공률 2/2는 **알려진 저장 계획의 open/seed11 로컬 native 실행**에만 해당한다. 새 LLM 협상에 따른 통신 효과, 다른 지도·시드, 실물 로봇, 일반화된 신뢰성은 검증하지 않았다. 원본 영상·대량 로그는 로컬 보관이며 이 요약과 해시는 원격 백업이 아니다.

`v17-concurrency-timeline-audit.json`은 두 실행의 단계·대기를 원본에서 재계산한다. 접근은 동시에 시작하지만 기본 `overlap_start=transit`은 빔 TRANSIT 전 상자 GRASP를 막아 첫39.34/반복26.29 SIM초 대기가 생겼다. 두 화물의 적재 운반 구간은96.51/8.82초 겹쳤으며, 기존 엄격한 실제 동시 이동 판정은11.1/2.2초였다. 전체 작업 직렬화가 아니라 단계 진입 시차와 유한 명령 공백·움직임 시점 차이가 함께 작용한다. 과거 grasp-admission 단회 진단은 제어기 수정도 달라 우열 비교나 현재 후보의 성공 근거로 사용하지 않는다.

v18 고정 소스 `81446dd0d3b97942b714442b086a4892a04b71b9`, 실행 번들 SHA-256 `a6891549ad88372a168fca34813ae96dd5c387447cc2ec4b352d3c96d5b891e4`는 공동 운반의 RGB 분석만 별도 CPU 프로세스로 옮겼다. 같은 open/seed11 저장 계획·입력의 native 전체 실행 2회 모두 빔·상자 물리 배치·방출과 프로토콜 종료가 성공했다(2/2). 첫 실행 wall 256.399초/SIM 251.680초, 반복 wall 204.848초/SIM 201.790초다. 실제 motion SIM/wall 비율은 0.99577/0.99999였고, 적재 동시 이동은 7.6/1.6 SIM초다. 새 LLM 호출·학습·weld는 0이다. 첫·반복 각각의 원본 해시, 실행기 source/input 불변, 원본 RGB+0.6초 만료를 지킨 갱신 명령, 별도 사후 물리 판정은 `independent-audit-v18-{full,repeat}.json`에 있다. 원본은 기본 checkout의 `outputs/simulation-realtime-20260923/native-v18-{full,repeat}`에 로컬 보관하고 TensorBoard `0923-실시간-진단/v18-{full,repeat}`에 별도 등록했다.

v18 운반 RGB 판단 age 중앙값은 0.316/0.282초, 판단 주기는 0.340/0.280초다. 첫 실행 값은 v17 반복의 0.303/0.280초보다 작지 않다. 첫 경유점도 v18에서 11.43/5.33 SIM초로 두 회차가 달랐다. v17 성공 2회의 wall 312.371/213.656초와 범위가 겹치므로 CPU 프로세스 분리만으로 일정한 주행 속도 개선이나 완주 시간 보장을 입증하지 않는다. 자식 프로세스에서 측정한 pair-bind 시간은 v17 스레드 측정값과 직접 비교하지 않는다. solo 판단은 510/600과 420/600으로 두 번 모두 한도 이내였으나 다른 조건의 여유를 보장하지 않는다.

첫 실행 중 실제 MuJoCo 관찰창에서 한글 상태 패널과 저장 계획 재생·새 자연어 메시지 0개를 확인했다(`communication-native-v18-live-ui.json`). 이 UI 확인은 물리 결과와 별개의 근거다. v18의 **2/2는 알려진 저장 계획의 open/seed11 로컬 조건**에 한정된다. 새 LLM 협상에 따른 통신 효과, 다른 배치·지도·시드, 실물 로봇과 일반화된 신뢰성은 검증하지 않았다. 기본 제어기 채택이나 후속 물리실험 예산은 이 기록만으로 승인하지 않는다.
