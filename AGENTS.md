# Project Guidance

## 로봇 제어 입력 — 2026-09-09

- 사용자 지시: 로봇의 관측 입력은 해당 로봇의 자기 시점 카메라와 위에서 보는 공용 카메라 영상으로 제한한다.
- 모델에 물체/로봇 좌표, 정답 위치, 접촉 판정, 관절 상태, 지도, 시뮬레이터 상태를 전달하거나 이 값으로 접근/파지/운반 행동을 보정하지 않는다. 고정 작업 설명과 명령 형식 설명은 관측 정보가 아니다.
- 평가용 정답은 별도 출력에만 기록하며 제어, 단계 전환, 성공 통보 입력에 사용하지 않는다. 실제 모델 요청의 이미지/텍스트를 보존해 입력 경계를 검증한다.
- `scripts/run_camera_pair_transport.py`가 이 제한을 적용하는 공동 운반 실행 경로다. 이전 `probe_dual_grasp_sync.py`는 정답 좌표 기반 물리 진단 기록이며 기본 실행을 차단한다. 사용자에게 카메라 기반 성공으로 제시하지 않는다.
- 사용자 추가 지시: 실제 카메라 배치와 화각을 유지한다. 집게를 보이게 하려고 카메라를 재배치하거나 외관·물체를 바꾸지 않는다. 행동 기억, 과거 두 카메라 영상 비교, 허용된 영상과 자기 발행 명령만으로 동작-영상 변화 학습을 적용하고 단점을 비교한다. 발행 명령을 실제 관절 상태나 동작 성공으로 취급하지 않는다. baseline과 이 기능을 사용하는 조건을 구분해 기록·감사한다.

## 교사 시연을 이용한 학습 — 2026-09-10 추가 승인

- 사용자 지시: “좌표같은 거 다 주면 성공하니까 그걸 바탕으로 학습 이어서 해보자.” 이번 학습 작업의 교사 시연 생성에서는 정답 좌표·관절/IK·접촉 정보를 사용해 성공 행동과 학습 표적을 만들 수 있다.
- 위 허용은 학습 데이터 생성용 교사에 한정한다. 학습된 학생의 독립 실행 입력은 자기 RGB·공용 top RGB·자기 발행 명령 이력이며, 실행 중 교사/정답 좌표/관절 측정/접촉/평가로 행동을 보정하지 않는다.
- 교사 원시 자료와 학생 입력·학습 산출물을 구분하고 데이터 출처를 기록한다. 교사 성공, 데이터 적합도, 시연 재생, 좌표 없는 학생의 새 조건 성공을 각각 구분해 검증한다. 카메라 배치/FOV 및 weld OFF 제약은 유지한다.

## 사전 지도 제공 — 2026-09-13 추가 승인

- 사용자 지시: 지형지물을 헤쳐 나갈 수 있도록 map을 구성하고 로봇이 확인·해결하는 능력을 추가한다. 지도 제공 방식은 “미리 만든 지도도 로봇에게 직접 제공”으로 명시적으로 선택했다.
- 이 지도 기반 실행 경로에서는 미리 작성한 정적 지도(좌표계, 경계, 통로, 고정 장애물·지형의 형상·높이·통행 조건, 목적 구역)와 지도에 맞춘 고정 카메라 보정 정보를 학생에게 제공할 수 있다. 기존 지도 입력 금지는 이 허용 범위에 한해 대체한다.
- 로봇/이동 물체의 실시간 정답 위치, 측정 관절, 접촉·성공 판정, 시뮬레이터 상태는 계속 제어 입력에서 제외한다. 현재 자기 위치와 변화는 허용된 자기 RGB·공용 top RGB 및 자기 발행 명령 이력에서 추정한다. 발행 명령은 이동 성공을 뜻하지 않는다.
- 지도와 물리 환경은 버전·해시로 연결한다. 지도와 현장 관측의 일치, 위치 추정, 경로 선택, 실제 통과·도착을 각각 검증한다. 지도를 따라 계산한 경로나 교사 주행을 로봇의 영상 기반 성공으로 보고하지 않는다.
- 기존 카메라 배치/FOV, 로봇 외관, weld OFF 제약은 유지한다. 지형 통과 가능성이나 하중별 성능을 실제 검증 없이 확정하지 않는다.

## 작업 기록과 저장 위치 — 2026-09-07 변경

- 사용자 지시에 따라 UGRP 프로젝트는 Google Drive를 사용하지 않는다. 상위 공통 지침의 Drive 저장 의무보다 이 프로젝트 예외를 우선한다.
- 작업 결과·참고 자료·검증 기록은 로컬 프로젝트에 저장한다. 코드 버전 관리는 기존 Git 절차를 따른다.
- 별도 요청이 없으면 이 프로젝트를 위해 Drive 조회·업로드·동기화·업로드 재시도·업로드 대기 기록을 수행하지 않는다.
- 기존 로컬 파일과 Drive 자료는 보존한다. Drive 사용 중단을 기존 자료 삭제 권한으로 해석하지 않는다.

## 프로젝트 프로세스 수명 주기

- UGRP 브리지, MuJoCo 워커, 하네스, 대시보드는 작업할 때만 시작한다. 작업이 끝나면 해당 작업에서 시작한 프로세스와 자식 프로세스를 종료한다.
- 장시간 실험은 실행 중이라는 이유만으로 자동 종료하지 않는다. 명시적인 작업/실험 종료 시점에만 정리한다.
- 새 로컬 지속 서비스는 기존 실행기가 전체 자식 정리를 보장하는 경우를 제외하고 반드시 `python3 scripts/ugrp_session.py run <이름> -- <기존 실행 명령>`으로 시작한다. 터미널에서 `Ctrl-C`하면 세션 전체가 정리되며, 다른 터미널에서는 `python3 scripts/ugrp_session.py stop <이름>`으로 같은 세션만 종료한다.
- 세션 래퍼로 시작하지 않은 기존 프로세스는 PID와 명령을 확인하기 전에는 일괄 종료하지 않는다. 다른 작업이나 사용자가 소유한 프로세스를 이름 패턴만으로 종료하지 않는다.

## GitHub 버전 및 실험 관리 — 2026-09-09

- GitHub를 코드의 기준으로 사용한다. 변경은 작업 브랜치에서 범위별 커밋과 PR로 남기고 관련 검증 후 main에 반영한다. 로컬 패치만 남긴 상태를 GitHub 반영 완료로 보고하지 않는다.
- 실험 전에 실행 코드를 커밋하고 결과를 해당 SHA에 연결한다. 코호트 실행 중에는 소스를 고정한다. 진단으로 원인을 좁힌 뒤 최종 후보의 전체 비교 검증을 수행한다.
- experiments/에 설정·환경·모든 결과·판정과 검토 범위·원본 위치와 해시를 기록한다. 성공률과 시간·행동 수·비용을 함께 평가한다.
- 파일 저장, 커밋, 푸시, PR, 병합, CI 통과를 각각 실제로 확인하고 구분해 보고한다. raw 영상·로그의 로컬 보관을 원격 백업으로 표현하지 않는다.
- 자세한 운영 절차는 CONTRIBUTING.md를 따른다.

## 실행 환경 정리 — 2026-09-09

- 사용자 요청으로 클라우드 GPU 시뮬레이션 배포 환경은 퇴역했다. 새 요청 없이 Lightning·Azure·Colab 시뮬레이션 설치나 자동 복구를 되살리지 않는다.
- 현재 시뮬레이션 Python은 `.venv-sim-worker-mac`이다. `.venv-sim`은 같은 환경의 호환 링크이며 별도 설치하지 않는다. 실물 로봇용 환경과 raw 실험 증거는 보존한다.
- 과거 코드·설정은 Git 이력과 `docs/cloud_simulation.md`를 참고한다. 과거 실행 환경 문서를 현재 설치 지침으로 사용하지 않는다.

## 파지 시험의 인위적 고정 제약 — 2026-09-09

- 사용자 지시: 앞으로 파지·공동 파지 시험에서는 물체를 그리퍼에 붙이는 weld 등 인위적 고정 제약을 기본적으로 끈다.
- 사용자가 명시적으로 요청한 제약 ON 비교 진단에만 보조 제약을 허용하고, 결과에 사용 여부를 표시한다. 접촉만으로 물체를 붙이거나 보조 제약 상승을 실제 파지 성공으로 보고하지 않는다.
- 접촉·마찰·관절 제한 등 정상적인 물리 엔진 제약을 제거하라는 뜻은 아니다. 기존 보조 제약 기반 실행기를 파지 검증에 쓰기 전에는 제약 OFF 경로인지 확인한다.
- `scripts/probe_dual_grasp_sync.py`는 기본 OFF이며, `--with-weld`는 명시적 비교 진단용이다.

## main 병합 승인 — 2026-09-09

- 사용자 요청으로 모든 파일의 코드 소유자는 `@kcm0127-dotcom`이며, 팀원의 PR에는 이 계정의 승인 리뷰가 필요하다.
- 이후 작업에서 PR 생성과 검증까지는 진행하되, main 병합은 사용자가 해당 PR을 확인하고 명시적으로 승인한 뒤 수행한다. 일반적인 구현·수정 요청을 병합 승인으로 간주하지 않는다.
- 관리자 계정으로 실행하는 에이전트도 사용자 확인 없이 관리자 우회 병합, 자동 병합 예약, 자체 승인 리뷰 제출을 하지 않는다. 소유자 본인 작성 PR의 자기 승인 제한은 사용자의 명시적 병합 승인 후 관리자 병합으로 처리한다.
- 승인 뒤 코드가 변경되면 변경 내용을 사용자에게 알리고 다시 승인받는다. 이 규칙을 설정하는 최초 PR의 반영은 해당 설정 요청에 포함된다.

## 병합 후 기본 로컬 프로젝트 최신화 — 2026-09-09

- 사용자 요청: 명시적으로 승인된 PR의 GitHub 병합을 확인한 에이전트는 같은 작업에서 기본 로컬 프로젝트 최신화까지 수행한다. 예약 작업은 만들지 않는다. 기존 main 병합 승인 규칙은 유지한다.
- 이 Mac의 기본 경로는 `/Users/changmin/projects/ugrp`이다. 다른 호스트에서는 프로젝트 설정과 `git worktree list`로 실제 기본 체크아웃을 확인한다. 별도 worktree에서 작업해도 병합 직전에 기본 경로의 최신 AGENTS.md를 읽는다.
- 기본 체크아웃의 저장소와 origin, main 브랜치, 미커밋·미추적 파일 부재를 확인한다. 실행 중인 실험·코호트·소스 고정이 필요한 다른 작업이 없음을 확인할 수 있을 때만 갱신한다.
- 해당 경로에서 `git fetch origin` 뒤 `git merge --ff-only origin/main`을 실행한다. 자동 stash, reset, clean, 강제 브랜치 전환 및 다른 에이전트의 feature worktree 갱신은 금지한다.
- 새로 가져온 origin/main과 로컬 HEAD가 같고 PR 병합 커밋을 포함하는지 확인한다. GitHub 병합 완료와 로컬 최신화 완료를 구분해 보고한다.
- 로컬 변경·분기·실험 때문에 갱신을 미룬 경우 경로와 이유를 보고하고 같은 작업의 다음 안전한 종료 시점에 재확인한다. 새 작업도 기본 경로의 최신 지침과 원격 대비 상태를 확인한 뒤 같은 안전 조건으로 최신화하고 작업 브랜치를 만든다.
