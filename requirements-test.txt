# Minimal, public-index dependencies for the offline regression suite.
# Keep simulation, hardware, and model-client dependencies out of CI.
numpy==2.5.2
opencv-python-headless==5.0.0.93
pytest==9.1.1
pillow==12.3.0
