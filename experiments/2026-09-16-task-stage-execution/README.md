# 단계 동기화 → 실제 구동 포트 연결 검증

2026-09-16. 실행 SHA `b11659c078f00fd099bcc28aa6d236b559772a44`.
고정된 [시험 계획](protocol.json)으로 6조건 모두 통과했다.
기존 PR55의 단계 계약에 실제 RGB 저장/보고 결합, 바퀴·팔 명령 유효시간,
공동 HOLD 및 새 보고 후 재개를 연결한 결과다.

**실제 영상·MuJoCo 구동 포트를 사용했지만, 단계 준비/완료 판정은 합성 입력이다.**
파지·들기·지지/해제 인식 또는 물체를 든 상태의 전체 운반 성공을 의미하지 않는다.
판별기 없는 기본 조건은 UNCERTAIN으로 명령을 발행하지 않는다.

| 조건 | 결과 | 접수된 로컬 명령 | 확인한 동작 |
|---|---|---:|---|
| absent_producer | 통과 / HOLD | 0 | 판별기 없으면 움직이거나 다음 단계로 넘어가지 않음 |
| five_stage_fixture | 통과 / FINISH | 10 | 합성 양쪽 DONE으로만 5단계 전환; 팔·바퀴 실제 물리 변화 |
| arm_hold_resume | 통과 / GO | 4 | 팔 대기 이동 취소, 한쪽 준비 차단, 양쪽 새 보고 후 재개 |
| carry_report_loss | 통과 / GO | 8 | 바퀴 제한 시간 만료, 보고 단절 HOLD, 새 보고 후 재개 |
| second_port_failure | 통과 / HOLD | 1 | 둘째 포트 제출 실패 시 먼저 접수된 팔 명령도 취소 |
| revoked_permission | 통과 / HOLD | 0 | 이전 허가로 명령 재발행 불가 |

243개 시험 단언 통과. 단계별 회귀 시험을 포함한 오프라인 suite는
**675 tests + 154 subtests 통과**(16.39초).
물리 평가에서 바퀴 시험의 각 로봇은 약 4.31mm/7.61mm 이동했고,
팔 정지·재개 시험의 각 로봇 관절 변화 최댓값은 약 0.347rad였다.
이 값은 모든 명령 뒤 평가 전용으로 산출했으며 제어에 사용하지 않았다.
세 입력 카메라의 고정 설정은 전후 동일했고 종료 시 활성 weld는 0개였다.

HOLD는 바퀴 명령을 0으로 만들고 마지막 발행 팔 목표를 유지한다.
관성·위치 제어의 정착 운동이 즉시 0이 되거나 물체가 유지된다는 판정은 하지 않는다.
GO/FINISH는 시험 종료 직전 동기화 상태이고, 이후 실행기 close로 정지·정리했다.
실물·장거리·적재·통신 버스·별도 프로세스 고장 복구는 이번 범위 밖이다.

## 기록과 확인

- [local-summary.json](local-summary.json): 모든 결과·환경·원본 224개 파일 SHA256.
- [input-audit.json](input-audit.json): 전달 요청 44개/원본 RGB 88장 및 자기 명령 이력 대조.
  해시·입력 경계 검사이며 합성 영상 판정의 정확도 검사는 아니다.
- `cases/`: 조건별 결과·평가 전용 샘플·사건 기록. 팔/바퀴 정지·재개 GIF와
  접촉표도 포함했다. 두 접촉표의 실제 카메라 영상과 전후 변화를 직접 확인했다.
- 원본 RGB/요청·응답/모델 XML 전체의 로컬 경로:
  `/Users/changmin/projects/ugrp-worktrees/task-stage-execution/outputs/task-stage-execution-b11659c`.
  원본 전체의 원격 백업을 의미하지 않는다. GitHub에는 위 선택 기록만 저장한다.
- 이 시험에서 시작한 세션과 자식 프로세스는 종료했다.

재현 방법과 실행 담당용 입력/출력은 [연결 문서](../../docs/task_stage_execution.md)를 참고한다.
GitHub Actions에도 같은 6조건 물리 시험을 추가했고 전체 실행 산출물을 14일 보존한다.
원격 실행 결과는 실제 Actions 결과로 별도 확인한다.
