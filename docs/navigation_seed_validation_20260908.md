# 신규 시드 검증 — 2026-09-08

요청: 시드 41에서 성공했던 정책이 여러 시드에서도 작동하는지 확인.

결과: 사전에 고정한 42–46 각 1회 실행에서 2/5 성공. 실패를 제외하거나 선택적으로 재시도하지 않았다. 로봇 1대의 운반 검증이며 다중 로봇 협업 성능을 검증한 것이 아니다. 표본이 작고 시드별 반복도 없어 일반 성공률을 확정할 수 없다.

| 시드 | 경로 | 결과 | 호출 | 입력 토큰 | 활성 시뮬레이션 초 |
|---|---|---|---:|---:|---:|
| 42 | B→C | 성공, 내려놓기 확인 | 20 | 101856 | 209.002 |
| 43 | A→B | 장애물 앞 반복 회전, 입력 예산 종료 | 22 | 115016 | 163.328 |
| 44 | B→C | 목적지 안 진입, 내려놓기 전 입력 예산 종료 | 23 | 117462 | 218.812 |
| 45 | C→A | 성공, 내려놓기 확인 | 20 | 100545 | 218.002 |
| 46 | A→B | 장애물 앞 반복 회전, 입력 예산 종료 | 23 | 117071 | 152.204 |

조건: 실제 Gemini 3.8 Flash, 호출 최대 30, 입력 최대 120000, 활성 시뮬레이션 최대 300초, impratio 10, noslip iterations 3, 통신 none. 종전 60000 토큰 조건의 성공을 의미하지 않는다. 3개 실패는 다음 요청 추정량을 포함하면 한도를 넘기 때문에 호출 전 중단되었다. 총 108회 호출, 보고된 입력 551950토큰. API 인프라 장애로 생략된 시드는 없다.

harness/sim/calibration 파일 해시를 각 실행 시작 전과 전체 완료 후 동결 manifest와 비교했다. 실행 정책 수정 없이 테스트했다. 검증 도구는 명시적인 expected_seed를 받도록 확장했고 기본 seed 41과 기본 입력한도 60000은 유지했다. 관련 검증기·cohort 테스트 38개 통과. 테스트 실행기와 검증기 소스는 결과 폴더의 validation-source에 보존했다.

시각 검토: 5개 영상 총 7734프레임 모두 디코딩, 각 영상의 시간순 12개 표본과 모든 108개 모델 nav 입력을 직접 확인했다. 성공 2건은 마지막 wrist 입력도 확인했다. 연속 영상 전체를 재생한 검토는 아니다. 각 run/manual-review.json에 검토 범위와 관찰을 남겼다. 운반 진행과 내려놓기 판정은 result.json의 물리·시각 gate 및 독립 budget-verification.json과 함께 해석해야 한다.

43/46에서는 목적지를 향해 다시 회전하면서 장애물이 전진 영역에 재진입하고, 반대 회전 후 다시 목표 방향으로 돌리는 패턴이 보인다. 단순히 토큰 한도만 늘려 해결될 것이라고 단정할 수 없다. 다음 개선 후보는 우회 방향을 직진 중에도 유지하고, 현재 RGB로 장애물을 통과했음을 확인한 뒤 목적지 정렬로 복귀하도록 모델의 장기 우회 메모를 보강하는 것이다. 그 수정 후에는 같은 실패 시드에서 회전 반복 감소를 확인하고, 별도 신규 시드에서 재검증해야 한다. 이번 검증에서는 이 개선을 적용하지 않았다.

산출물: [전체 결과](../outputs/navigation-seed-validation-20260908/summary.json), [사전 조건](../outputs/navigation-seed-validation-20260908/protocol.md), [동결 manifest](../outputs/navigation-seed-validation-20260908/validation-manifest.json), [독립 감사](../outputs/navigation-seed-validation-20260908/audit-summary.json). 각 solo-42~46 폴더에 원본 결과, 결정 로그, 모델 입력 이미지, motion-1x.mp4, nav-qa.jpg, motion-qa.jpg를 보존했다. 종전 성공 정책 소스는 outputs/navigation-trials-20260908/final-source에 보존돼 있다.

종료: 세션 gemini-unseen-seeds-20260908 종료 및 소유 프로세스 그룹 33837 잔여 0개 확인. cleanup.json에 기록했다. 프로젝트 예외에 따라 모든 기록은 로컬 저장이며 Drive 작업은 하지 않았다.

독립 감사 결과: 성공 42/45는 14개 자동 검사 모두 통과했고, 나머지 3개는 release/finish 및 성공 gate가 통과하지 않았다. 다섯 run의 source-manifest가 동일했다. 장애물·동료 침투 2mm 초과 기록은 모두 0초였다. 43에는 stale 응답 2건이 거부된 기록도 있어 예산 손실에 영향을 주었다. 성공의 의미는 이번 운반 과제의 목적지 구역 내부 지면 배치이며, 정밀 목표점·자세 정합은 아니다(42/45의 별도 cargo evaluation.success는 false, 위치 오차 약 0.275/0.285m).
