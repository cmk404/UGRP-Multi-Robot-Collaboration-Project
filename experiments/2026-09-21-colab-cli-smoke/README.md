# Colab CLI CPU 실행·회수 진단 — 2026-09-21

실행 SHA: `ebff3ee367c0879b38596577053a65bbc3a51694`. Colab CLI 0.6.0의 기존 인증으로 CPU 세션을 만들고, 프로젝트 CLI로 실제 Git SHA를 보존한 소스를 업로드했다. 원격 Python 3.12 환경에서 `scripts.sim_quickstart`를 실행한 뒤 결과 ZIP과 SHA-256을 CLI로 회수하고 내부 파일 해시까지 검증했다. 종료 후 이 작업이 만든 Colab 세션을 중지했다.

- 물리 진단 1회 중 1회 통과, 실패 0회. R1 이동 0.138548m, 시뮬레이션 1.5초, 12프레임.
- 원격 작업 wall time 40.47초. 설치/전송 시간은 포함하지 않는다.
- 모델 호출 0회. 고정 바퀴 명령 데모이며 자율 행동 수·LLM 운반 성공률의 측정이 아니다.
- CPU 런타임, 유료 자원 구매 없음. 실제 계정 과금/컴퓨팅 유닛 차감은 별도로 계측하지 않았다.
- 영상 0·5·11번 프레임의 장면·R1 카메라가 정상 렌더링되고 변화하는 것을 직접 확인했다. 전체 프레임별 물체/접촉 감사는 하지 않았다.

![검토한 세 프레임](visual-check.png)

`run.json`은 소스·환경·명령·종료 코드·파일 해시, `summary.json`은 물리 진단 결과다. `manifest.json`에 원본 위치·해시·검토 범위를 기록했다. raw 소스 압축본·GIF·전체 로그·결과 ZIP은 로컬에 보관하며 GitHub 원격 백업이라고 주장하지 않는다. 실물·다중 로봇 운반·ACT GPU·EGL·외부 LLM·Kaggle 원격 실행의 검증이 아니다.
