# 목적지·지형 변화 E2E 검증

실험 전 조건은 `protocol.json`에 고정한다. V1~V6 모두 실제 세 LLM의 새 공동 계획부터 시작한다. 역할·경로·의존성은 모델들이 정하며 목적지만 작업 조건으로 지정한다. 기존 운반 스킬이나 물리 파라미터는 수정하지 않는다.

각 조건 1회인 탐색 검증이며 성공률 일반화로 해석하지 않는다. 좁은 통로 사전 거부는 운반 실패로 기록하고 장애물 회피 성공으로 세지 않는다. 결과는 코호트 종료 후 추가한다.
