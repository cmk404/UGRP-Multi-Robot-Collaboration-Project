# Active pixel identification and end/axis servo — 2026-09-10

User asked to solve the remaining camera-only grasp failure. Prior v5 raw own images do not show jaws; overhead model tips are only a few pixels apart and sometimes plainly wrong. The old LK path also required a texture-bearing target point on the uniform beam. These are verified perception problems, not evidence that camera-only grasp is impossible.

First diagnostic: fixed isolated open/close twice and look +/-50 from existing startup. Source e4268de; raw `/Users/changmin/projects/ugrp/outputs/jaw-visibility-20260910-r1`. Zero model calls. Camera geometry, scene and weld OFF unchanged. Open/close differences revealed 2 principal compact finger-motion lobes; repeated open/close gave consistent centers and axis. The anchor denotes the moving gripper region, not exact tips. Look round-trip tracking ended within .008 pixel of the fresh anchor; this is a limited tracking check, not grasp evidence.

Candidate: identify gripper from issued servo-1 action and actual pixel change, track that limited region with forward/back flow; segment visible orange contours each frame; select a beam by image-derived anchor/continuity; align the gripper region to a visible beam endpoint and its opening axis across the beam width. Greedy bounded raw-command trials are accepted only after observed combined-error decrease. Reject/reverse worsening candidates with raw actions, never position resets. Trial closure requires two alignment observations plus visible own-view beam. Small trial lifts are checked for object-follow evidence across both images; the physical referee remains output-only.

This is a diagnostic low-level pixel-servo controller, not independent language-model cooperation. Both camera JPEGs and own issued actions are the entire observation interface. No simulator state or output evaluation enters policy, transitions or stopping. Baselines remain intact. Exact image/action/observation/history replay is required. Every runtime source is committed before execution and frozen during the run. Begin r1/80 fixed rounds, inspect original images/overlays and physical output. Only broaden when prerequisite behavior is exercised. Preserve failures, source SHA, all evaluation results and raw hashes. No Drive use; PR review before merge.

First pixel candidate 6446e2a r1/80: exact80-call replay passed; error decreased from~103 to~20 in combinedpixelunits, no trialclosure/contact/lift. Periodic fresh motion measurements changed the grippercenter/axis relative to propagated estimates; tracking roundtrip alone was insufficient for action scoring. Next candidate remeasures open/close after every test and rollback, so acceptance compares fresh isolated gripper measurements. Track-only improvement is neveraccepted. Runr1/160 next; bothcameras/scene/controlboundary unchanged.

826c2fa r1/160 completed77.683s and exact160-call replay passed, but blocked at17 after one forward candidate: a low-contrast JPEG bridge merged two finger regions at the new pixel alignment. Stronger contrasts21..25 repeatedly resolve collinear balanced support. The next tracker retains normal calibration and only uses stronger-threshold fallback when at least3 thresholds agree on compact center/axis. Original probe unchanged; actual failed004->005 becomes valid with center(.37587,.63173), axis(.0381,.99927), 9points. Syntheticbridge/motion/occlusion/broadmotion regressions pass. Test r1/160 again from newlycommittedfixedsource.

e588d64 r1/160 completed78.526s and exact160-call replay passed. Reached combinedcost~31 then one base-turn candidate lost measurable opening-axis geometry; repeated gripper commands blockedat55. Actual042->043 t15 formed54/11/52pixel components withPCA ratio1.67; thresholds17..20 consistentlyseparate2balancedlobesaxis~(.18,.98),span~10.4px. Next candidate applies the existing consensus fallback to invalid base geometry too, withnoaxisconfidence relaxation. Controller also reverses a candidate after6 unsuccessfulmeasurementsteps ratherthan retrying the same unobservable view. Originalconstraintsunchanged; nextfixedr1/160 diagnosescontinuedalignment.

4dfd4c2 r1/160: exact replay passed, 79.296 seconds, no blocked state. Fresh measurement recovered throughout the run and minimum combined cost reached 25.29, but there were no trial closures or physical contacts. Original own-view images show the target disappears by round 23 and remains absent. The acceptance objective did not penalize this loss. Next candidate retains own-view target visibility and adds horizontal own-image endpoint aiming, with stronger top-view opening-axis priority. These are explicit image-space heuristics to test, not measured 3D pose. Run a fixed 200-round r1 diagnostic before deciding whether a two-robot comparison is meaningful.

adbaed9 r1/200: exact replay passed; no physical contact. Original own frames at57 and60 show the same target, but the nearest-image-center heuristic switches from its near endpoint (.759,.673) to its opposite endpoint (.139,.386) after look+100. Next candidate carries the previous own endpoint as the reference across pans, updating that reference only on open observations. This fixes the demonstrated discontinuity; it does not assume camera calibration or prove grasp. Repeat fixed r1/200 with all other parameters unchanged.

7f42bef r1/200: exact replay passed. A fresh open observation at193 reached distance20.97px, opening-axis error-.033rad, own horizontal error1.78px, improving both direction and own endpoint alignment. The policy was still searching at the budget end; there were no trial closures or contacts. Next diagnostic extends the same unchanged policy to600 fixed rounds (runner upper bound1000). This tests convergence, not a new controller candidate, and retains every earlier failure. No evaluation-dependent termination or feedback.
