# 동일 물체 공동 파지 동기화 진단

요청: 두 로봇이 같은 물체를 잡고, 한쪽 파지가 늦으면 기다린 뒤 공동 상승하는지 시험한다.

## 기존 실행 경로 진단

- 코드 SHA: `4190db90e4a724a3f960b7d3a8494f931bc7aaf4`
- 환경: `.venv-sim-worker-mac`, macOS, seed 11, `UGRP_BEAM_DYNAMIC=1`.
- 명령: `python scripts/record_sim_beam_mission.py --out outputs/dual-grasp-sync-20260909/baseline.mp4` (ugrp_session 래퍼 사용).
- 결과: 파지 전 scout r2 이동 중 r3와 간격 0.003 m가 최소 0.03 m보다 작아 `PeerTooClose` 실패. 공동 파지 또는 동기화 성공을 검증하지 못함.
- 기본 structural 실행 경로는 물체 pose를 직접 변경하므로 이 시험에 사용하지 않았다.
- 원본 로그/영상: `outputs/dual-grasp-sync-20260909/`에 로컬 보관. 원격 백업되지 않음.
- 영상 마지막 프레임 확인: 실제 파지 단계에 도달하지 않음.
- 회귀 검사: 188 passed, 107 subtests passed. 물리 파지 검증과 별도 결과.

## 분리 시험

이동과 scout 단계를 제외한 초기 배치에서 실제 접촉을 검사한다. 파지 후 사용하는 weld는 보조 제약이며 실제 마찰 파지나 하드웨어 성공을 증명하지 않는다. 실험 결과는 후속 기록에 포함한다.

## 분리 시험 결과

- 실행 SHA: `df4eeb083c94ede8d5e0198fc3dd86ea6b238036`. 동일 소스는 전용 브랜치의 `c300035` 커밋에도 보존된다. 실행 당시 다른 작업의 untracked 파일 두 개는 사용하지 않았다.
- Python 3.12.13, MuJoCo 3.12.0, seed 11, 두 조건 각각 1회, 외부 LLM 호출 0회/비용 0.
- 동시 파지: 상승 0.055545 m, 최종 기울기 6.2518도, 최종 SIM 시각 4.96 s.
- R3 파지 2초 지연: 상승 0.055569 m, 최종 기울기 6.2684도, 최종 SIM 시각 7.79 s.
- 지연 조건: R1 접촉 확인 및 제약 활성화 후 2 SIM초 대기, R3 접촉 및 양쪽 재확인 후 상승. 대기 끝 물체 중심 높이 0.019811 m (초기 약 0.020 m).
- 판정: 실제 접촉 확인을 거쳐 공동 상승하는 분리 진단 2/2 완료. production 동기화 제어기나 LLM 협업 성능 검증은 아니다.
- 두 로봇은 초기 fixture에 배치되며 실행 중 payload pose 쓰기 없음. 양쪽 접촉 후 weld 보조 제약을 사용하므로 마찰만으로 지지하는 파지 성공을 의미하지 않는다.
- 검토: 지연 조건 영상의 1초 간격 contact sheet에서 접근, 대기, 상승 확인. 최종 기울기는 측정값이며 연속 최대 기울기/미끄러짐/장기 유지/내려놓기/실패 복구를 검증하지 않았다. 영상 끝 hold는 정지 화면이며 물리 유지 시험이 아니다.
- raw 영상·로그는 로컬에만 보관, 해시와 원본 위치는 artifacts.json. 결과 JSON은 저장소에 포함. 해당 시험 세션 종료 확인.
