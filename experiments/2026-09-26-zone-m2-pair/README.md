# 2026-09-26 M2: 실행 중 GT 없는 2인 long_beam 운반

브랜치 `claude/zone-m2-pair`, PR #203(draft). workflow `zone-m2-pair` → `scripts/run_m2_pair.py`. 문서: [docs/zone_m2_pair.md](../../docs/zone_m2_pair.md).

## 질문

두 로봇이 **자기 손목 RGB, 정적 태그 지도, 개략 주문서, 자기 발행 명령**만으로 빔까지 접근하고, 정렬·파지·들기·운반·내려놓기까지 끝낼 수 있는가? pair study(#200)의 `stub_approach`는 사전 정류장까지 GT stub를 썼고, `own_only`는 0.2–0.48 m 뒤에서 출발했다. M2는 두 로봇을 창고 출발점(지도 spawn)에서 출발시키고 실행 중 GT를 전혀 쓰지 않는다.

## 입력 경계

- 로봇 입력은 다음뿐이다. 이 목록은 결과의 `controller_inputs`와 같다.
  - 자기 `robot_cam` JPEG와 자기 발행 명령
  - 정적 태그 지도 `zone_wide_door_tags_v2`와 loop-v2 위치 추정 보정(#201 e10f88d, 읽기 전용 복사)
  - 개략 주문서
    - 빔 설정 자세를 **0.10 m / 10° 격자로 반올림**한 값. 설정 자세 자체는 주지 않는다.
    - 역할, 운반 구간, 속도(pair study 과업표)
  - 승인된 `PairCarrySync` 장벽(자기 frame id)
  - (플래그) 후보 상태 채널. 사용자 결정 대기 중이며 ON과 OFF를 따로 보고한다.
- 정적 keep-out은 두 가지다. 주문서 빔 발자국에 격자 오차 패드 0.06 m를 더한 것, 그리고 상대의 주문서 정류장·사전 정류장(과업표에서 나오는 정적 정보)이다.
- 평가 전용 파일에만 적는 것:
  - 로봇·빔 자세, 위치 추정 오차(`localizer-eval.jsonl`)
  - 손가락 힘, 접촉
  - 실패 전파 지표
- 러너의 `--on-failure continue`(M2 기본)는 상대를 멈추지 않는다. pair study의 "첫 실패에 둘 다 정지"는 러너만 아는 정보여서 `halt_all` 비교용으로만 둔다.
- weld OFF. `cargo_noslip_v1`이 주 프로필이며 사용자 결정을 기다린다.

## 가져온 의존성

`imports.json`에 원본 PR·커밋·sha256을 적었다. 가져온 파일은 모두 바이트 동일하며 수정하지 않는다.
- #200 29700fd: 쌍 빔 study v3와 인식·hold v3
- #201 e10f88d: 위치 추정기, 주행기 v1/v2, 태그 지도, 보정
- #202 8f2aa31: 시나리오 실현 가능성 검사

M2에서 새로 쓴 코드는 다음 세 가지다.
- `sim/zone_tagged_cargo_scene.py`
- `harness/pair_owncam_approach.py`(M1 `OwnCamDriverV2` 하위 클래스: 최종 heading, 대칭 envelope, 위치+heading 도착)
- `scripts/run_m2_pair.py`(`PairStudent` 하위 클래스: `approach` → `wait_approach` 장벽 → pair study v3 단계)

## 1단계 dev (열린 바닥, 서쪽 pickup 바닥)

| run | 소스 | 채널 | 결과 | 최종 오차 | 도착 r1/r2 (SIM s, 둘러보기) | 도착 시 실제 위치-실제 사전 정류장 | 도착 시 위치 추정 오차 |
|---|---|---|---|---|---|---|---|
| dev1-701-on | a91a24b 작업본 | ON | 성공 | 0.045 m | 39.9 (2) / 61.4 (3) | 0.052 / 0.009 m | 0.019 / 0.033 m |
| dev1-702-off | a91a24b 작업본 | OFF | 성공 | 0.030 m | 44.1 (2) / 50.1 (2) | 0.076 / 0.060 m | 0.017 / 0.011 m |
| dev1-703-on | 78b2020 | ON | 성공 | 0.030 m | 38.1 (3) / 47.5 (2) | — | — |
| dev1-703-off | 78b2020 | OFF | 성공 | 0.030 m | 38.1 (3) / 47.5 (2) | — | — |

- 4회 모두 로봇-로봇·접근 중 로봇-빔·벽 접촉 0회, 파지 전 빔 이동 0.0001 m, weld 0이었다.
- 주문서 오차는 3.6 cm와 2.2–2.5°였다.
- 주행 중 위치 추정 오차의 p50은 3–6 cm, 최대는 0.17–0.22 m였다(회전 중 과도 오차; 둘러보기 뒤 회복).
- 전체 SIM 시간은 106–120 s, wall 143–152 s, 부하 평균 4.3–5.4였다.
- 성공한 실행에서는 채널이 결과를 바꾸지 않는다(703 ON과 OFF가 같다).
- dev에서 조정한 값은 없다. 첫 설계 그대로 4/4.

## 1단계 사전 등록 (test, 동결 전 작성)

- **seed:** 711–718. `gen_stage1_seeds.py`(rng 20260926)로 조건을 거른 8개이며, 목록은 `stage1_test_scenarios.json`에 있다.
  - 빔: x 0.35–0.85, y −1.9–−0.4, yaw −0.6–0.6
  - 출발 오프셋: ±0.1 m, ±0.4 rad
  - 거르는 조건: 두 사전 정류장이 계획 가능할 것, 운반 끝이 칸막이에서 0.30 m 이상 떨어질 것
- **팔:** 채널 ON 8회 + OFF 8회. `--on-failure continue`, `--hold-check fullframe_v3`, `cargo_noslip_v1`.
- **실행:** 동결 커밋의 분리된 detached worktree에서 한다. 스레드는 1개, SIM은 동시에 최대 2개이며 부하 평균을 기록한다.
- **관문**(팔별로 판정하고 따로 보고):
  - **M2-S1(주):** `success_gt` ≥ 6/8. 두 로봇 done, 들어 올림 > 0.06 m, 바닥에 놓음, 계획 대비 최종 오차 ≤ 0.10 m.
  - **M2-S2 접근:**
    - 두 로봇이 모두 `arrived`한 실행이 ≥ 7/8일 것.
    - 모든 도착에서 실제 위치와 실제 사전 정류장의 거리가 ≤ 0.10 m, yaw 오차 ≤ 0.10 rad일 것(평가 전용).
  - **M2-S3 안전:** 모든 실행에서 다음을 만족할 것.
    - 로봇-로봇 접촉 표본 0, 접근 중 로봇-빔 접촉 표본 0
    - 파지 전 빔 이동 ≤ 0.02 m
    - weld 0
  - **M2-S4 입력:** 모든 결과가 `gt_at_runtime: false`이고 `dirty_source: false`일 것. 경계 테스트(`tests/test_pair_owncam_approach.py`)가 통과할 것.
  - **비용 보고:** SIM 시간, 명령 수, frame 수, 둘러보기 수, wall 시간, 부하.
- **중단 규칙:**
  - 코호트 중에는 소스를 바꾸지 않는다.
  - 한 팔의 첫 4회가 모두 실패하면 그 팔을 멈추고 "중단"으로 보고한다.
  - 예외로 끝난 실행은 실패로 센다.
  - 재실행은 t=0 이전 인프라 오류일 때만 하며, 그 경우도 기록한다.
- **해석 범위:**
  - 열린 바닥 한 지도, 빔 한 종류, 로봇 2대의 결과이며 문 통과(2단계)는 포함하지 않는다.
  - 성공 실행에서는 ON과 OFF 차이가 나타나지 않을 것으로 예상한다. 채널 효과는 3단계(실패 전파)에서 본다.

## 1단계 결과 (동결 3fdf011, detached worktree `zone-m2-pair-s1-frozen`)

원본은 로컬 `outputs/zone-m2-pair-20260926/stage1-3fdf011/`에 있다(gitignored, 원격 백업 아님). 집계는 `results-stage1-3fdf011.json`이다. 16회 모두 `gt_at_runtime: false`, `dirty_source: false`이며, 부하 평균은 3.95–6.79였다.

| 관문 | ON | OFF | 판정 |
|---|---|---|---|
| M2-S1 성공(`success_gt`) | 8/8 | 8/8 | **통과** |
| M2-S2 접근 | 두 로봇 도착 8/8; 실제 사전 정류장까지 최대 0.111 m(711), 0.110 m(716) | 같음 | **실패**(≤ 0.10 m 기준 초과 2개 seed) |
| M2-S3 안전 | 로봇-로봇 0, 접근 중 로봇-빔 0, 파지 전 빔 이동 ≤ 0.0001 m, weld 0 | 같음 | **통과** |
| M2-S4 입력 | GT 없음, 깨끗한 소스, 경계 테스트 통과 | 같음 | **통과** |

- 최종 오차는 0.023–0.042 m, 들었을 때 최대 기울기 ≤ 1.1°였다. 평균 비용은 SIM 115.3 s, 명령 1253, frame 733, 둘러보기 4.1, wall 176 s.
- **S2 실패의 구성:**
  - 로봇이 자기 목표(주문서 사전 정류장)에 도착한 오차는 최대 0.062 m다.
  - 도착 시 위치 추정 오차는 최대 0.031 m, yaw 오차는 최대 0.082 rad다.
  - 0.10 m를 넘긴 것은 주문서 격자 오차(711 0.057 m·yaw 0.03 rad, 716 0.064 m)가 더해졌기 때문이다. 관문을 실제 사전 정류장 기준으로 정했으므로 사전 등록대로 실패로 적는다.
  - 두 seed 모두 이후 자기 RGB 정렬이 잔차를 흡수해 성공했다.
- **관문 밖 발견:** 712·713·716·718에서 로봇-벽 접촉 표본이 각 1개 나왔다.
  - 모두 t≈1.3 s 출발 직후 r2였다. 생성기의 출발 오프셋(x −0.06~−0.10 m)이 서쪽 벽(x −1.025)에 섀시 모서리를 붙였다.
  - 접근 제어가 아니라 시나리오 배치 문제다. 다음 생성기는 출발 자세에도 벽 여유 검사를 넣는다.
- 성공 실행에서는 ON과 OFF의 결과·명령 수가 같았다(채널은 실패가 없으면 행동을 바꾸지 않는다).
- **범위:** 열린 바닥, 서쪽 pickup 바닥, 빔 한 종류, 로봇 2대. 문 통과는 없다.
- **TensorBoard:** snapshot `0926-zone-m2-pair-stage1`(20 runs = test 16 + dev 4), view 키 `zone_m2_pair_stage1_20260926`. 기준선은 pair v3(`0926-zone-owncam-pair-v3`)이다. 공용 서버에서 확인했다.

## 3단계 사전 등록: 실패 전파 (ON/OFF, 동결 전 작성)

- **질문:** 한 로봇이 운반 중 실패했을 때 상대 로봇이 자기 입력만으로 어떻게 반응하는가? 러너는 상대를 멈추지 않는다(`--on-failure continue`). pair study v3는 첫 실패에 러너가 둘 다 멈춰서 이 차이를 잴 수 없었다.
- **seed:** 721–724. `gen_stage3_seeds.py`(rng 20260927)로 만들었고, 뽑는 범위와 거르는 조건은 1단계와 같다. 목록은 `stage3_test_scenarios.json`에 있다.
- **실험자 개입:** seed마다 한 로봇의 집게를 연다. 로봇 명령이 아니며 평가 전용으로 기록한다.
  - 721 r1 3.0 s, 722 r2 3.0 s, 723 r1 6.0 s, 724 r2 6.0 s(운반 시작 뒤 시간)
- **팔:** ON 4회 + OFF 4회. hold check `fullframe_v3`, `cargo_noslip_v1`, weld OFF.
- **측정:** 모두 평가 전용 `failure_propagation`에 남긴다.
  - 첫 실패 로봇·이유·시각
  - 상대의 종료 상태·실패 이유·첫 실패 뒤 종료까지 지연
  - 상대가 첫 실패 뒤 낸 바퀴 명령 수, 마지막 바퀴 명령 시각
  - 실패 뒤 빔 최대 기울기, 최종 빔 높이
- **가설 판정:**
  - **M2-P1:** 피해 로봇이 자기 영상으로 낙하를 감지(`LOAD_CHANGED_IN_CARRY`)한다. 8/8, 개입부터 감지까지 ≤ 1.5 s.
  - **M2-P2(ON):** 상대가 `PARTNER_ABORT`로 멈춘다. 4/4, 첫 실패 뒤 ≤ 0.5 s, 그 뒤 바퀴 명령 0.
  - **M2-P3(OFF):** 상대는 실패를 알 수단이 장벽 제한 시간뿐이다. 그래서 4회 중 ≥ 3회에서 첫 실패 뒤에도 바퀴 명령을 낼 것으로 예상하고, 그 양과 종료 방식을 보고한다.
- **해석:**
  - ON/OFF 차이는 **실패 뒤 상대 행동의 차이**로만 보고한다. 채널 채택 여부는 사용자 결정이다.
  - 낙하 전에 실패한 실행(개입 미적용)은 "측정 불가"로 기록하며 대체하지 않는다.
- **중단 규칙:** 코호트 중 소스 변경 없음. 예외로 끝난 실행은 기록하며, 재실행은 t=0 이전 인프라 오류일 때만 한다.
- **dev 확인(dev3-701-drop, 사전 등록 전, 작업본):** r1의 집게를 운반 3 s 뒤에 열었고, 0.9 s 뒤 r1이 `LOAD_CHANGED_IN_CARRY`로 감지했다.
  - ON: r2가 같은 제어 tick에 `PARTNER_ABORT`로 멈췄고, 그 뒤 바퀴 명령은 0이었다.
  - OFF: r2가 16.5 s 동안 바퀴 명령 160개로 운반을 계속했다. 빔 기울기는 최대 6.1°였고, 36.7 s 뒤 `BARRIER_LOWER_TIMEOUT`으로 끝났다.
  - dev 뒤 수정은 평가 전용 한 가지다. 루프 종료 시 마지막 정답 표본을 1개 추가했다(ON에서 실패 뒤 빔 기울기가 비어 있었다).
- **소스:** 동결 커밋(아래 결과에 적음)의 `open_floor` 경로는 1단계 동결본(3fdf011)과 같은 동작이다. 추가된 문 단계 코드는 `--stage door`에서만 실행된다.

## 3단계 결과 (동결 5f74873, detached worktree `zone-m2-pair-s3-frozen`)

원본은 로컬 `outputs/zone-m2-pair-20260926/stage3-5f74873/`, 집계는 `results-stage3-5f74873.json`이다. 8회 모두 `gt_at_runtime: false`, 깨끗한 소스였고 부하 평균은 4.0–5.3이었다.

| seed | 개입 | ON: 상대 반응 | OFF: 상대 반응 |
|---|---|---|---|
| 721 | r1 3 s | 0.0 s에 `PARTNER_ABORT`, 뒤 바퀴 명령 0 | 16.6 s 동안 바퀴 명령 162개로 운반 계속, 36.8 s 뒤 `BARRIER_LOWER_TIMEOUT` |
| 722 | r2 3 s | 0.10 s에 `PARTNER_ABORT`, 같은 tick 명령 1(실패 전에 낸 것, 시각 반올림 차이) | 162개 / 16.6 s, 36.8 s 뒤 lower 장벽 시간 초과 |
| 723 | r1 6 s | 적용 안 됨: r2 접근 실패 → r1 접근 장벽 150 s 초과 → r2 즉시 `PARTNER_ABORT` | 적용 안 됨: r1 접근 장벽 초과 13.1 s 뒤 r2 `APPROACH_TIMEOUT` |
| 724 | r2 6 s | 0.10 s에 `PARTNER_ABORT`, 0 | 133개 / 13.8 s, 34.0 s 뒤 lower 장벽 시간 초과 |

- 적용된 낙하는 6/6 감지됐다(0.70–0.90 s).
- 실패 뒤 빔 최대 기울기는 ON 5.4–5.5°, OFF 6.1–6.2°였다. 빔 최종 높이는 둘 다 0.028–0.032 m(바닥)다.
- **사전 등록 판정:**
  - M2-P1 **실패**(8/8 요구, 723은 측정 불가)
  - M2-P2 **실패**(4/4 요구; 723 측정 불가, 722 같은 tick 명령 1)
  - M2-P3 **통과**(3/3)
  - 측정 가능한 실행만 보면 차이는 분명하다. ON에서 상대는 0.1 s 안에 멈췄고, OFF에서 상대는 절반만 쥔 빔을 13.8–16.6 s 더 끌고 가다가 34–37 s 뒤에야 멈췄다.
  - 채널 채택은 사용자 결정이다.
- **723 접근 실패의 원인**(평가 전용 `localizer-eval.jsonl`로 분석):
  - r2가 최종 heading까지 약 π 회전과 주행을 동시에 했다.
  - t≈25 s에 yaw 추정 오차 0.43 rad, 위치 오차 0.76 m로 벗어났다. 그 뒤 std ≈ 0.05–0.065의 **확신 있는 틀린 모드**(오차 0.94 m)에 머물렀다.
  - M1 무부하 둘러보기 규칙(std > 0.05면 다시 보기, 0.15 넘어야 lost)이 20번 둘러보기만 반복하는 livelock이 됐다.
  - 1단계 16회와 3단계 나머지를 합치면, 접근 v1은 시나리오 12개 중 11개에서 두 로봇 모두 도착했다.
- **TensorBoard:** snapshot `0926-zone-m2-pair-stage3`(10 runs = test 8 + dev 2), view 키 `zone_m2_pair_stage3_20260926`. 기준선은 pair v3 낙하 641–644(러너가 첫 실패에 둘 다 정지)이며, 공용 서버에서 확인했다.

## 접근 v2 (3단계 723 뒤, dev)

- **v1 실패 원인**(723): 최종 heading까지 약 π 회전과 주행을 동시에 했다.
  - loop-v2 운동 모델은 heading을 동쪽으로 유지한 채 맞춘 것이라, 회전과 주행이 겹친 움직임을 작게 예측했다.
  - 추정이 0.94 m 틀린 모드에 머물렀고, 둘러보기 livelock이 이어졌다.
- **초안(b70a284, dev4-723, 실패):** 출발점에서 먼저 제자리 회전했다.
  - r2 spawn은 서쪽 벽에서 0.16 m라 회전하면 벽을 보고 태그가 안 보였다.
  - 제자리 회전을 0.67 rad 작게 예측했다.
  - 태그 없는 둘러보기도 fix로 세서 livelock이 반복됐다.
- **v2(0bb4d6e):**
  - 주행을 시작할 때 추정한 heading을 유지한 채 이동한다.
  - 사전 정류장 0.30 m 안에서만 제자리 회전하며, 0.8 rad마다 멈춰서 둘러본다.
  - 태그를 못 본 둘러보기는 fix가 아니다. fix 없는 둘러보기가 3번이면 새 위치 추정기로 다시 초기화하고(최대 2회), 그래도 안 되면 `lost`로 끝낸다.
  - dev5-723(open floor): 성공, 최종 오차 0.036 m. r2는 103.7 s, 둘러보기 6번에 도착했다.
- v2는 2단계에서만 쓴다. 1단계·3단계 결과는 v1의 결과다.

## 2단계 dev (문 `door_1` 0.50 m 통과 운반)

| run | 소스 | 설계 | 결과 |
|---|---|---|---|
| dev2-801 | b70a284 (v2 초안) | 한 번에 2.2 m 운반 | r2 접근 실패(v2 초안 문제) |
| dev5-801 | 0bb4d6e | 한 번에 2.2 m; 정렬 동안 dead reckoning | 성공(오차 0.188 m). 그러나 파지 시 추정이 1.37 m 틀렸고, clamp에 걸린 보정이 우연히 회전 표류를 상쇄했다 |
| dev6-801 | 076cf53 | + 파지 전 정지 재위치 추정(오차 1.0–4.5 cm) | 문 통과, 교차 오프셋 +0.118 m, 최종 오차 0.326 m → 실패 |
| dev6-802 | 076cf53 | 같음 | 편대가 −0.30 rad 돌아 r2가 문기둥에 충돌(벽 접촉 191표본) → 실패 |
| dev7-801 | 5a09a74 | + 체크포인트 분할 운반 | **성공**: 최종 오차 0.118 m, 교차 오프셋 +0.040 m, 벽 접촉 0 |
| dev7-802 | 5a09a74 | 같음 | **성공**: 최종 오차 0.155 m, 교차 오프셋 −0.098 m, 벽 접촉 0 |

- **발견 1:** loop-v2 운동 모델은 팔을 내린 정렬 펄스(0.3 s)를 약 4배 크게 예측한다(r1 명령 적분 0.84 m·s, GT 이동 0.29 m). 그래서 정렬 뒤, 파지 전에 멈춰 선 채 새 위치 추정기로 다시 초기화한다(M1 넓은 둘러보기).
- **발견 2:** 들고 있는 동안 손목 영상에는 빔만 보인다(외부 단서 없음). 편대 heading은 개루프에서 0.06–0.1 rad/m씩, 방향도 일정하지 않게 표류한다.
- **분할 운반 설계:**
  - 주문서 체크포인트(빔 x 1.55, 2.40; 각각 r2가 문 직전, r1이 문 직전인 위치)에서 멈춘다.
  - 둘이 함께 내려놓고 집게를 연 뒤 hover로 올리고, 각자 자기 둘러보기로 다시 위치를 추정한다.
  - 같은 팔 자세로 다시 잡고 들어 올린 뒤, 문 축으로 다시 정렬하고 다음 짧은 구간을 운반한다.
  - 구간 거리는 주문서 값이라 두 로봇이 같다. 장벽 키는 구간마다 새로 만든다.
- 문 정렬 명령 대비 실제 가로 이동 비율은 0.29–0.97로 흩어져 있어, 보정 상수를 넣지 않았다.

## 2단계 사전 등록 (동결 전 작성)

- **seed:** 811–816. `gen_stage2_seeds.py`(rng 20260928)로 만들었고, 목록은 `stage2_test_scenarios.json`에 있다.
  - 빔: x 0.85–1.10, y 0.05 ± 0.10, yaw ± 0.10
  - 출발 오프셋: ±0.1 m, ±0.4 rad
  - 거르는 조건: 출발 섀시가 서쪽 벽에서 0.03 m 이상 떨어질 것, 두 사전 정류장이 계획 가능할 것, #202 검사기 판정이 `feasible`일 것
- **팔:** ON 6 + OFF 6. `--stage door --approach v2 --on-failure continue`, `fullframe_v3`, `cargo_noslip_v1`, weld OFF.
- **실행:** 동결 커밋의 detached worktree에서 한다. 스레드 1개, SIM 최대 2개, 부하 기록.
- **관문**(팔별):
  - **M2-D1 성공:** ≥ 4/6. 두 로봇 done, 들어 올림, 바닥에 놓음, 빔 x ≥ 2.7, 계획(3.20, 0.05) 대비 최종 오차 ≤ 0.25 m.
  - **M2-D2:** 모든 실행에서 로봇-벽·빔-벽 접촉 표본 0.
  - **M2-D3 접근:** 두 로봇이 모두 도착한 실행 ≥ 5/6.
  - **M2-D4:** GT 없음, 깨끗한 소스, 로봇-로봇 접촉 0, weld 0.
  - **보고:** 교차 오프셋, 구간별 보정, 재위치 추정 오차, 비용.
- **중단 규칙:** 1단계와 같다.
- **해석 범위:** 문 하나(0.50 m), 빔 한 종류, 서쪽→동쪽 한 방향. 체크포인트는 주문서에 적힌 정적 정보다.

## 2단계 결과 (동결 fa682a6, detached worktree `zone-m2-pair-s2-frozen`)

원본은 로컬 `outputs/zone-m2-pair-20260926/stage2-fa682a6/`, 집계는 `results-stage2-fa682a6.json`이다. 12회 모두 `gt_at_runtime: false`, 깨끗한 소스였고 부하 평균은 4.1–6.2였다.

| 관문 | ON | OFF | 판정 |
|---|---|---|---|
| M2-D1 성공 | 1/6 (812) | 1/6 | **실패** |
| M2-D2 벽 접촉 0 | 0 | 0 | 통과 |
| M2-D3 접근 | 6/6 | 6/6 | 통과 |
| M2-D4 입력·안전 | 813·816에서 로봇-로봇 접촉 | 같음 | **실패** |

- 성공한 812는 최종 오차 0.066 m, 교차 오프셋 −0.003 m였다. 평균 비용은 SIM 224–239 s, 명령 2358, wall 373–392 s.
- **실패 원인**(평가 전용 파일로 분석):
  1. **811·815·816** — r2의 세 번째 파지(체크포인트 2, 칸막이 동쪽)를 `GRIP_NOT_SEEN`으로 판정했다.
     - 영상에서는 띠가 집게 사이에 있었다. 동쪽의 어두운 조명에서 아래쪽 빔 띠 비율이 0.038–0.040으로, 기준 0.04를 조금 밑돌았다(인식 거짓 음성).
     - 위쪽 띠는 0.92–0.94였다.
  2. **813** — 두 로봇의 접근 경로가 교차해 t≈27 s에 접촉했다(105표본).
     - r1의 위치 추정이 0.57 m 틀린 모드로 '도착'했고, 정렬이 search↔p45를 오가다 `ALIGN_TIMEOUT`이 났다.
     - 816도 같은 시각에 접촉했다(6표본).
  3. **814** — r2(x 1.5, 서쪽을 봄)의 파지 전 둘러보기 2번이 std 0.092·0.085로, 기준 0.06을 넘었다.
- ON/OFF 차이: 실패 뒤 ON은 상대가 `PARTNER_ABORT`로 바로 멈췄고, OFF는 lift 장벽 시간 초과로 끝났다.
- **TensorBoard:** snapshot `0926-zone-m2-pair-stage2`(14 runs = test 12 + dev 2), view 키 `zone_m2_pair_stage2_20260926`. 기준선은 1단계이며, 공용 서버에서 확인했다.

## 문 v2 (dev, 위 원인별 수정; `--door-version v2`)

1. **첫 파지 자세:** 도착 추정 + 자기 RGB로 본 빔 끝의 이동(정렬 전·후의 grip 점과 축 heading)으로 계산한다. 멈춰서 둘러보기가 필요 없다(814).
2. **체크포인트에서 다시 잡기 전 자기 RGB 정렬을 다시 한다:** 마지막 grip 거리 자세부터 시작하며, 빔이 집게 안에서 밀리는 것을 막는다(811 가설).
3. **체크포인트 둘러보기:** ±72°로 넓혔다(814).
4. **파지 확인:** 띠가 화면을 채우고, 빔 색이 위 또는 아래 1/3에 있으면 된다.
   - 기록된 파지 영상 125장을 재생하면 거부 0건이다(기존 기준은 4건 거짓 음성).
   - 놓친 파지 영상은 기록에 없어서 특이도는 검증하지 못했다. 들어 올린 뒤 co-motion 확인은 유지한다.
5. **접근-빔 일관성:** 정렬의 첫 빔 보기가 예상(0.46 m 앞, 옆 0)에서 0.25/0.20 m 넘게 벗어나면, 새 위치 추정기로 다시 접근한다(1회)(813).
6. **주문서 창고 칸 배정:** 역할별 계획 경로 길이 합이 최소가 되게 정한다(정적). 문 시나리오에서는 모두 바꿔 배정되어 경로 교차가 없다(813·816).

## 문 v2 dev 경과와 최종 구성

| run | 소스 | 결과 |
|---|---|---|
| dev9-813/814 | a100464 이전 (체크포인트 재정렬, 제자리) | 813: 체크포인트 1 재정렬 시간 차 27 s > lift 장벽 20 s. 814: 체크포인트 2에서 제자리 재정렬 60 s 실패(띠가 잘림) |
| dev10-813/814 | a100464 (0.15 m 후진 뒤 재정렬, 장벽 60 s) | 체크포인트 1은 통과, **체크포인트 2 재정렬이 둘 다 실패**. 문 근처·동쪽의 어두운 바닥에서 띠 인식이 바닥과 섞였다(`BAND_CLIPPED`↔`NO_BAND_END_ESTIMATE`) |
| dev11-811/813 | 80cd7b2 (저장된 grip 점으로 다시 잡기 + `grip_view_m2`) | **둘 다 성공**: 최종 오차 0.114 / 0.088 m, 벽·로봇 접촉 0. 811의 세 번째 파지는 아래 0.041 / 위 0.918로 통과 |

- **재해석:** 코호트의 세 번째 파지 실패는 빔이 밀린 것이 아니라 동쪽 조명에서 생긴 인식 거짓 음성이었다. 자기 RGB 빔 인식(v2 임계값)은 칸막이 동쪽의 어두운 바닥에서 약하다. 이것은 남은 한계다.
- **문 v2 최종 구성:**
  - 첫 파지 자세: 도착 추정 + 자기 RGB 빔 변위
  - 체크포인트에서는 저장된 grip 점으로 다시 잡고, 파지 확인은 `grip_view_m2`로 한다
  - 체크포인트 둘러보기 ±72°, 체크포인트 뒤 lift 장벽 60 s
  - 접근-빔 일관성 확인, 주문서 창고 칸 배정(최소 경로)

## 2b단계 사전 등록 (문 v2, 동결 전 작성)

- **seed:** 821–826. `gen_stage2b_seeds.py`(rng 20260929)로 만들었고, 조건은 2단계와 같다. 창고 칸을 바꿔 배정하므로 출발 벽 여유는 두 칸 모두에서 확인했다. 811–816은 이제 문 v2의 dev seed다.
- **팔:** ON 6 + OFF 6. `--stage door --approach v2 --door-version v2 --on-failure continue`, `fullframe_v3`, `cargo_noslip_v1`, weld OFF.
- **관문:** 2단계와 같은 M2-D1(≥ 4/6), D2, D3(≥ 5/6), D4. 팔별로 판정한다.
- **중단 규칙·실행 조건:** 1단계와 같다. 동결 detached worktree, 스레드 1개, SIM 최대 2개, 부하 기록.

## 2b단계 결과 (동결 ed15489, detached worktree `zone-m2-pair-s2b-frozen`) — **미완료 코호트**

사용자 결정으로 M2가 Kiro에 인계되어 코호트를 중간에 멈췄다. 결과는 `results-stage2b-ed15489.json`(`build_results.py stage2b stage2b-ed15489 --allow-incomplete`, SHA-256 `788ba6fa…ffb`)에 있다.

| seed | ON | OFF | 최종 오차 (m) |
|---|---|---|---|
| 821 | 성공 | 성공 | 0.154 |
| 822 | 성공 | 성공 | 0.119 |
| 823 | 성공 | 성공 | 0.089 |
| 824 | 실패: r2 `LOAD_NOT_HELD_AFTER_LIFT`, r1 `PARTNER_ABORT` | 실패: r2 `LOAD_NOT_HELD_AFTER_LIFT`, r1 `BARRIER_CARRY_TIMEOUT` | 0.793 |
| 825 | 누락 (실행 도중 정지) | 누락 (실행 도중 정지) | — |
| 826 | 누락 (실행 안 함) | 누락 (실행 안 함) | — |

- **관문:**
  - M2-D1(≥ 4/6): 완료분 3/4이고 남은 2개가 결과를 바꿀 수 있어 **판정 불가**.
  - D2(벽 접촉 없음): 완료분에서 **통과**.
  - D3(≥ 5/6 도착): 완료분 4/4 도착, 나머지 누락으로 **판정 불가**.
  - D4: 완료분에서 **통과**.
- **비용(완료분 평균):**
  - ON: SIM 245.1 s, 명령 2495.8.
  - OFF: SIM 248.9 s, 명령 2495.8.
  - 부하 평균은 `cohort.log`에 있다(4.5–7.5, 다른 에이전트 `kiro-cli` 부하 포함).
- **2단계(fa682a6, 1/6)와 비교:** 같은 seed가 아니므로 직접 비교는 아니다. 다만 문 v2가 2단계의 네 가지 실패 원인(격벽 동쪽 파지 시야 거짓 음성, 접근 경로 교차 충돌, 사전 파지 sweep 표준편차, 체크포인트 재정렬)을 dev와 이 완료분에서 재현 없이 넘겼다.
- **824:** 들기 직후 r2가 빔을 놓쳤다(`LOAD_NOT_HELD_AFTER_LIFT`). 파지 위치·접촉 원인은 **분석하지 않았다.** 상대 r1의 반응은 조건에 따라 달랐다. ON은 상태 채널로 곧바로 abort했고, OFF는 운반 장벽 시간 초과로 끝났다. 3단계 관찰과 같은 방향이다.
- **정지 경위 정정:** 인계 지시에 따라 코호트 루프(PID 44061)만 멈추려 했다. 그러나 `ugrp_session`은 주 자식이 끝나면 그룹 전체를 정지시키므로, 실행 중이던 825 ON/OFF SIM도 함께 멈췄다. 이 때문에 825에는 `result.json`이 없고 826은 시작하지 않았다. `cohort.log` 12:12:22 줄은 825가 계속 도는 것처럼 읽히는데, 이는 잘못이다. 정정 줄을 같은 로그 끝에 덧붙였고, 원래 줄은 보존했다.
- **TensorBoard:** snapshot `0926-zone-m2-pair-stage2b-incomplete`(13 run: test 8 + dev 5, `collection.json` SHA-256 `9646bc2d…155`), view key `zone_m2_pair_stage2b_20260926`. 공용 서버(PID 9291)에서 run 13개와 824 ON `reported_success=0`, 821 OFF `sim_s=252.3` 값을 확인했다.
- **원본:** `outputs/zone-m2-pair-20260926/stage2b-ed15489/`(로컬, gitignore. 원격 백업 아님).

---

# Kiro 인계 이후 (2026-09-26, 브랜치 `kiro/zone-m2-pair-v3`, PR #205, base `claude/zone-m2-pair`)

`kiro/` 접두사는 Kiro의 작업이다. Claude의 worktree(`zone-m2-pair`, `zone-m2-pair-s2b-frozen`)와 원본 `outputs/zone-m2-pair-20260926/`은 읽기만 했다. Kiro의 raw 원본은 `outputs/zone-m2-pair-kiro-20260926/`(로컬, gitignore, 원격 백업 아님)에 있다.

## 2b단계 완료 기록 (동결 ed15489, Kiro 자기 detached worktree `ugrp-wt/kiro-m2-pair-s2b-frozen`)

- **825:** 인계 정지로 t≈206.9 s(두 번째 구간 운반 중)에 멈췄다. `result.json`이 없다. 사전 등록 중단 규칙은 "재실행은 t=0 이전 인프라 오류일 때만"이므로 **재실행하지 않고 `interrupted`로 기록한다.** 결과를 대체하지 않는다.
- **826:** 한 번도 시작하지 않은 사전 등록 실행이라 재실행이 아니다. 같은 동결 소스 `ed15489`, 같은 명령(`--stage door --approach v2 --door-version v2 --on-failure continue`)으로 ON/OFF 한 번씩 실행했다(12:18:19 시작, 부하 9.21/6.96/6.49, `stage2b-ed15489-completion/cohort.log`). 둘 다 `dirty_source: false`, `gt_at_runtime: false`.

| seed | ON | OFF | 최종 오차 | 도착 r1/r2 (SIM s) | SIM 시간 | 명령 |
|---|---|---|---|---|---|---|
| 826 | 성공 | 성공 | 0.120 m | 47.3 / 114.5 | 259.4 s | 2767 |

- 벽·빔-벽·로봇-로봇 접촉 0. 집계: `results-stage2b-ed15489-completion.json`.
- **2b 최종 판정(팔별, ON과 OFF 같음):** 821·822·823·826 성공, 824 실패, 825 interrupted.
  - M2-D1(≥ 4/6): 4/6 — 825를 실패로 세어도 **통과**.
  - M2-D3(≥ 5/6 도착): 5/5 완료분 도착 — 825를 미도착으로 세어도 **통과**.
  - M2-D2·D4: 완료분 5회에서 통과. 825는 접촉 통계가 없어 판정 불가.
  - 코호트는 형식상 **미완료(6개 중 1개 interrupted)** 로 남긴다.

## 824 원인 분석 (평가 전용 파일·자기 영상 재생)

- r2의 **세 번째 파지**(체크포인트 2, 로봇 x 2.84, 문 동쪽 어두운 바닥). 파지 전후 GT:
  - 두 로봇 손가락 5.48 N, 빔 z 0.0607 m까지 들림, 기울기 0.03°.
  - 빔의 r2 기준 상대 위치 (0.437, −0.011) m가 파지·들기 전후 같다. **파지는 성공했다.**
- 들기 확인(`ob2.co_motion_signature`, IoU ≥ 0.45)이 IoU **0.0**을 냈다. 영상 하단 60 %의 빔 아래 면이 hue 41, S 166, **V ≈ 103**(5–95 백분위 32–116)으로, v2 빔 색 기준 V ≥ 120 아래였다. 파지 영상 서명 166 px, 들기 영상 0 px. 서쪽 바닥의 같은 면은 V ≈ 221이었다.
- → **인식 거짓 음성.** 2b의 822 r2(0.479), dev11-811 r2(0.528)도 같은 위치(x ≈ 2.9)의 세 번째 들기에서 기준에 겨우 걸쳤다.
- 따라서 "체크포인트 다시 잡기 뒤 파지 위치 / 접촉"은 원인이 아니다.

## 문 v3 (`--door-version v3`, dev)

- **변경은 하나:** 들기 co-motion 서명의 빔 색 기준을 V ≥ 60으로 낮췄다(`harness/owncam_pair_lift_v3.py`). hue 25–54, S ≥ 100, 영역·팽창·IoU 기준 0.45는 v2와 같다. 파지 띠(V ≤ 60 and S < 90)는 S 조건 때문에 섞이지 않는다(테스트). 운반 중 hold check(`owncam_pair_hold_v3`), 파지 시야 판정, 나머지 문 v2 구성은 그대로다.
- **오프라인 재생**(`replay_lift_v3.py`, 기록된 모든 들기, GT 라벨은 채점에만 사용):
  - M2 원본 64회 186개 들기(모두 GT held): v2 거짓 음성 2(824 ON/OFF), **v3 0**, v3 최솟값 0.558.
  - pair study(#200) 원본 50회 87개 들기: v2·v3 모두 0, v3 최솟값 0.751.
  - **특이도(빔 없이 들기)는 기록이 없다.** 그래서 실험자 개입 `--inject-open-at-lift <robot>:<segment>`를 추가했다. 해당 구간의 들기부터 시뮬레이터 집게를 OPEN으로 유지하고, 로봇의 발행 명령 이력은 바꾸지 않는다(CLOSED 그대로, 평가 전용 기록).
- **러너 정리(사용자 결정 2026-09-26):** `--status-channel` 기본값 `on`, help의 "CANDIDATE … pending user decision" 삭제, `off`는 장벽만 쓰는 진단. 결과의 `contact_profile_note`는 `cargo_noslip_v1` 승인을 적는다. 동결 버전(ed15489 등)은 고치지 않았다.
- **평가 전용 추가:** `unplanned_drops`(내리기 단계가 아닌데 빔 z가 0.045 m 위에서 0.035 m 아래로), `makespan_sim_s`(두 로봇 종료 상태의 마지막 SIM 시각).

## 2c단계 사전 등록 (문 v3, dev 전에 작성·커밋)

- **seed:** 831–838. `gen_stage2c_seeds.py`(rng 20260930)로 만들었고, 뽑는 범위와 거르는 조건은 2b와 같다. 목록은 `stage2c_test_scenarios.json`에 있다. 821–826과 811–816은 v3의 dev seed로 쓸 수 있다.
- **팔:**
  - **주 팔 ON:** 831–836, `--stage door --approach v2 --door-version v3 --status-channel on --on-failure continue`, `fullframe_v3`, `cargo_noslip_v1`, weld OFF. 상태 채널은 모든 조건의 인프라다(사용자 결정).
  - **진단 팔 OFF:** 같은 seed, `--status-channel off`. 연구 조건이 아니라 장벽만 쓰는 진단이다.
  - **개입 팔:** 837(`--inject-open-at-lift r2:2`, 문 동쪽 세 번째 들기), 838(`r1:0`, 첫 들기), ON만.
- **관문**(주·진단 팔별로 판정):
  - **M2-D1 성공 ≥ 4/6**, **D2** 로봇-벽·빔-벽 접촉 0, **D3** 두 로봇 도착 ≥ 5/6, **D4** GT 없음·깨끗한 소스·로봇-로봇 접촉 0·weld 0. 정의는 2단계와 같다(빔 x ≥ 2.7, 최종 오차 ≤ 0.25 m).
  - **M2-L1 들기 민감도:** 모든 주·진단 실행의 들기 중 GT held(빔 z > 0.03 m, 자기 손가락 둘 다 > 1 N)인데 IoU < 0.45인 것이 0개.
  - **M2-L2 특이도:** 개입 2회 모두 개입한 들기에서 피해 로봇이 `LOAD_NOT_HELD_AFTER_LIFT`로 실패.
  - **M2-L3:** 개입 2회 모두 상대가 ≤ 0.5 s 안에 `PARTNER_ABORT`.
  - **보고:** seed별·팔별 성공, 도착, 계획 외 낙하, SIM makespan, 명령 수, wall, 부하.
- **중단 규칙:**
  - dev 뒤 동결 SHA를 이 README에 적고 detached worktree에서 코호트를 **한 번** 돌린다. 코호트 중 소스를 바꾸지 않는다.
  - 한 팔의 첫 4회가 모두 실패하면 그 팔을 멈추고 "중단"으로 보고한다.
  - 예외로 끝난 실행은 실패로 센다. 재실행은 t=0 이전 인프라 오류일 때만 하며 기록한다. 중간에 끊긴 실행은 `interrupted`로 남기고 대체하지 않는다.
  - dev에서 v3 설계(V ≥ 60, IoU 0.45)를 바꾸면 이 절에 변경과 근거를 덧붙인 뒤 동결한다. seed·관문·중단 규칙은 바꾸지 않는다.
- **해석 범위:** 문 하나(0.50 m), 빔 한 종류, 서→동 한 방향, 로봇 2대. 2b와 seed가 달라 직접 비교가 아니다. 대화 효과가 아니라 실행 인프라(자기 RGB 문 통과) 검증이다.

## 2c dev (사전 등록 뒤, 동결 전; dev seed만)

| run | 소스 | 결과 |
|---|---|---|
| dev12-824-v3-on | 9b0ca56 | **성공**, 최종 오차 0.071 m. 들기 6개 IoU 0.837–0.922(2b에서 0.0이던 r2 세 번째 들기 0.837). 벽·로봇 접촉 0 |
| dev12-811-v3-on-openlift (r2:2) | 9b0ca56 | 개입 적용(218.3 s). r2가 그 들기에서 IoU **0.137**로 `LOAD_NOT_HELD_AFTER_LIFT`, r1은 0.1 s 뒤 `PARTNER_ABORT`, 그 뒤 바퀴 명령 0 |
| dev13-822-v3-off | 9b0ca56 | 성공, 0.130 m |
| dev13-813-v3-on-openlift (r1:0) | 9b0ca56 | 개입 **미적용**. 접근 경로가 교차해 로봇-로봇 접촉 105표본, r1 접근-빔 불일치로 재접근, r2 lift 장벽 초과 |

- **9b0ca56의 결함:** 창고 칸 배정 규칙(`depot_assignment`)이 `door_version == 'v2'`에서만 실행돼 v3가 문 v2 구성을 다 쓰지 않았다(dev12·dev13 네 번 모두 칸 교환 없음; 2b 824는 교환). 813은 2단계 코호트에서 이 규칙이 생긴 원인 seed다. 이 dev 네 번은 **칸 배정이 빠진 v3**의 결과로만 기록한다.
- **수정:** `a.door_version in ('v2', 'v3')`, 그리고 v3가 v2 분기를 모두 타는지 확인하는 회귀 테스트. v3 인식 설계(V ≥ 60, IoU 0.45)는 바꾸지 않았다.
| dev14-813-v3-on-openlift (r1:0) | 2b14185 | 칸 교환 적용, 접촉 0. 개입 적용(134.2 s), r1이 첫 들기에서 IoU **0.175**로 감지, r2 0.0 s 뒤 `PARTNER_ABORT`, 바퀴 명령 0 |
| dev14-824-v3-on | 2b14185 | 칸 교환 적용, **성공** 0.082 m, 들기 6개 IoU 0.756–0.867, 접촉 0 |

- dev 요약: GT held 들기에서 v3 IoU 최솟값 0.756(dev12·13·14 성공분), 개입한 들기 0.137·0.175. 기준 0.45는 그대로 둔다.
- **동결:** 이 기록을 담은 커밋(코드는 2b14185와 같다)을 detached worktree `ugrp-wt/kiro-m2-pair-s2c-frozen`에서 `run_stage2c_cohort.sh`로 한 번 실행한다. 원본은 `outputs/zone-m2-pair-kiro-20260926/stage2c-<SHA>/`.
