# ㄱ자 자세의 공동 파지 시험

사용자 요청: 몸체는 앞으로 향한 상태에서 팔을 옆으로 돌려, 손잡이 없는 물체 본체를 두 로봇이 잡는다. 인위적 고정 제약은 사용하지 않는다.

설계: 물체는 기존 5 x 45 x 4 cm 직육면체, 0.196 kg. 긴 축은 world Y, 로봇 몸체 전방은 world X, 팔은 각자 +90/-90도로 안쪽을 향한다. 이때 그리퍼 닫힘 방향은 world X가 되어 물체의 5 cm 폭을 끼워 잡는다. 기존 정면 접근은 닫힘 방향이 물체의 긴 축과 같았다.

초기 배치는 분리 시험 fixture이며 자율 접근 검증은 아니다. 시험 중 물체 pose 변경, 추가 손잡이, 인위적 고정, 마찰/질량 조정은 하지 않는다. 실행 SHA와 실제 접촉·상승·유지 결과, 원본 영상 위치 및 해시는 실행 후 기록한다.

## 결과

- 코드 SHA `9bb4131` (전체 SHA는 manifest), Python 3.12.13 / MuJoCo 3.12.0 / macOS, seed11, 조건별1회. `--side-grasp` 사용. LLM 호출/비용 0.
- R1/R3 몸체 yaw 초기0, 팔 yaw 명령 +90/-90도 (PWM2500/500). 접은 상태에서 회전 후 뻗고 내려가 파지. 기존 마찰·질량·모터 설정 유지.
- 동시 파지: 최종 상승0.071614m, 최종 기울기0.0001도.
- R3 파지2초 지연: 최종 상승0.071601m, 최종 기울기0.0018도.
- 두 조건 모두 상승 후 2 SIM초 실제 물리 유지. 0.1초 간격20개 표본에서 양쪽 로봇 양쪽 손가락 접촉과 3cm 이상 높이를 만족. 모든 이벤트의 beam constraints_active=false. 최소 높이/최대 표본 기울기는 summary.json.
- 지연 조건1초 간격 영상 프레임을 검토해 몸체 방향 유지, 팔 측면 회전, 본체 직접 파지, 상승/유지를 확인. 영상 끝 정지 프레임과 별도로 물리2초를 진행했다.
- 판정: 이 fixture에서 비보조 공동 파지/짧은 유지2/2 완료. 실물 보정, 자율 접근, 다른 초기조건, 긴 유지, 내려놓기와 미끄러짐 복구는 미검증.
- 회귀 검사188 passed,107 subtests passed. 세션 종료 확인. raw 영상·로그는 로컬 outputs/side-grasp-20260909에만 보관; 결과JSON/해시는 Git에 포함.
