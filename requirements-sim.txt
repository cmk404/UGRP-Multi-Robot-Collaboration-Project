# Simulator development dependencies (Python 3.12; Mac and Ubuntu 24.04).
# Ubuntu setup, OS libraries and no-model demo: docs/ubuntu_quickstart.md
# This is not an exact lockfile of the historical N7 experiment.
# Current environment: .venv-sim-worker-mac; .venv-sim is a compatibility symlink.
# Install: python3.12 -m venv .venv-sim-worker-mac
#          .venv-sim-worker-mac/bin/pip install -r requirements-sim.txt
mujoco==3.12.0
numpy==2.5.2
opencv-python-headless==5.0.0.93
pillow==12.3.0
websockets==15.0.1
glfw==2.10.2
PyOpenGL==3.1.10

# --- optional V2 training / policy service dependencies ---
# torch==2.13.0            # Oracle has the +cpu wheel: pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# stable_baselines3==2.9.0
# gymnasium==1.3.0
