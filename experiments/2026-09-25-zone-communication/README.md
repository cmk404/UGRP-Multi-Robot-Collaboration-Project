# 2026-09-25 구역 벤치마크 통신 비교 (ZC)

사전 등록: [protocol.md](protocol.md). 모든 결과는 교사 실행기 조건이다.

## ZC1 시작 조건 — 실패, LLM 실행 안 함 (소스 `759d84d`)

fixture 6회(seed 12). 원본은 `outputs/zone-communication-20260925/gate-*`(로컬만)에 있다.

| 조합 | 결과(심판) | 완료 SIM초 | 교사 결과 |
|---|---|---:|---|
| independent nominal | 실패(과잉 배달) | 278.5 | 배치 7, 동료가 가져감 4 |
| independent graspfail | 6/6 | 403.0 | 배치 6, 파지 실패 1, 동료가 가져감 10, **막힘 2** |
| plan_first nominal | 6/6 | 156.0 | 배치 6 |
| plan_first graspfail | 실패(5/6) | 156.0 | 배치 5, 파지 실패 1 |
| dynamic nominal | 6/6 | 154.0 | 배치 6 |
| dynamic graspfail | 6/6 | 177.0 | 배치 6, 파지 실패 1 |

independent graspfail의 교사 막힘 2건 때문에 시작 조건을 통과하지 못했다. 사전 등록대로 LLM 실행을 시작하지 않았다.

원인(평가 전용 재생 기록): 집게가 열린 채로 남은 주입 파지가 세 번 시도하는 동안 red-2 상자를 동쪽으로 27 cm(x 1.00 → 1.27) 밀었다. 밀린 상자는 red-3의 잡기 전 지점 위에 놓였다. r2는 red-2의 금지 구역과 겹친 채로 다음 작업(red-3)을 받았다. 교사 경로 계획은 "경로가 없을 때만 겹친 여유를 푼다" 규칙에 따라 상자를 밀며 앞으로 나아갔고, 약 250 SIM초 동안 red-2를 밀었다. red-3 작업(목표 지점이 점유됨)과 cyan-2 작업 모두 120초 제한으로 끝났다. r1은 같은 red-3를 9번 선언했지만 "먼저 배정된 로봇 우선" 규칙으로 매번 멈췄다. 같은 현상이 개발 실행 dev4-ind-inj2에서도 같은 시각에 나타났다.

수정(`34ceefe`): 출발점이 상자 금지 구역과 겹치면, 상자를 밀기 전에 먼저 가장 가까운 빈 칸으로 물러난다. 이동 목표가 다른 상자의 금지 구역 안에 있으면 10초 뒤에 `teacher_path_blocked`(goal_occupied)로 끝낸다.
