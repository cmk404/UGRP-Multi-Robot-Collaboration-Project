# 추가 속도 개선 — 2026-09-24

사용자의 native MuJoCo 임무 시간 단축 요청을 대상으로 한다. 화면 배속이 아니라 동일한 물리·카메라 조건에서 실제 운반 완료 시간을 비교한다. 기준 v19 grasp 두 실행은 200.929/196.104초, 두 화물 배치·방출 및 프로토콜 종료 성공이다.

`baseline-time-breakdown.json`은 기존 원본의 단계 시간을 분해한다. 상자 carry 약76 SIM초 중 명령 사이 공백이 있고, 긴 직선에서 .08 상한에 도달한다. 빔 미세 접근은51–54 SIM초이며 작은 보정 명령 반복이 주원인이다. 이 분석은 새 후보의 성공이나 속도 개선 결과가 아니다.

`protocol.json`에 최대3회의 유한 native 실행을 기록한다. v20은 solo 이동 판단 사이에서 마지막 승인 명령을 원본 영상 TTL 이내에 갱신하는 단일 변경이다. 다음 후보는 기존 포트 범위 안에서 먼 상자 경로의 전진/측면 상한만 높인다. 각 후보는 커밋과 새 번들 후 실행하며, 실패도 원본·감사·TensorBoard에 보존한다. 파지 정렬 기준·카메라·물리·지도 경로·.6초 RGB TTL·.25초 명령 상한·하역 자원 독점은 유지한다.

v20 고정 소스 `9b846e7`는40.211초에 coarse 접근120회 한도로 종료됐다. 물리 시계는0.99985배속이었고 solo carry/renewal은0회여서 갱신 성능을 판정할 수 없다. `audit-v20-full.json`은 소스/입력 불변·원본 해시 및 실패를 기록한다. TensorBoard `0923-실시간-진단/v20-full`에 실패를 보존했다.

`coarse-handoff-diagnosis.json`은 실패 마지막 RGB와 v19 성공 마지막 RGB2개를 기존6개 미세 정렬 모델에 다시 넣은 결과를 담는다. 세 프레임 모두 지원 범위를 통과했다. v21은 기존 coarse 전진 거리 안에서6개 모델의 fine 지원을 확인하고, 정지 후 새 RGB로 거리/지원을 다시 확인한 뒤 원래7단계 미세 정렬로 넘기는 대체 진입이다. coarse yaw/lateral ready를 충족했다고 표시하지 않으며 최종 정렬·파지 기준을 유지한다. 수정된 상자 운반 속도와 함께 남은2회 유한 예산에서 전체 완주를 검증한다. 현재 v21 새 물리 완주는 아직 검증하지 않았다. 결과는 이 문서에 실행 SHA·번들·원본·시간·한계를 연결해 추가한다. 원본은 기본 checkout의 `outputs/simulation-realtime-20260924-faster`에 로컬 보관한다.


v21 고정 소스 `d2c4e8b`는25.274초에 정지 영상의 초기 바퀴 방향 추정 실패로 종료됐다. 별도 기록 `audit-v21-full.json`과 TensorBoard `0923-실시간-진단/v21-full`에 보존했다. 새로운 정지 영상과 그 다음 영상 모두 기존6개 미세 모델은 지원했지만, 초기 방향 추정 실패가 별도로 관측 가능한 RGB 전방 간격까지 누락시켜 handoff를 막았다.

v22는 충분한 바퀴 픽셀·화물·참조 영상에서 전방 간격을 별도 보존한다. 초기 방향 추정이 없으면 coarse 제어는 계속 무효/정지이며, yaw를 포함한 더 정밀한6개 모델이 초기·새 정지 영상 모두를 지원할 때만 기존 미세 정렬로 넘긴다. 다른 영상 판정 실패는 중단한다. `protocol.json`의 근거 검토에 따라 전체 native wall 예산1800초를 유지하며 v22 최종2회(첫 성공 시 반복)를 검증한다. 아직 새 완주 성공을 주장하지 않는다.


v22 full은158.784초에 coarse120회 소진으로 종료됐고, 같은 제어 구간이0.3003배속으로 실행됐다. 다음70초 진단은 동일 실행 번들(문서만 다른 Git SHA)에서0.99988배속으로 돌아와 coarse 이후 fine forward 단계까지 도달했으나 지정 시간에 중단했다. MuJoCo 창을 앞으로 올리는 조작은 실행되지 않아 창 위치 원인 가설은 검증하지 않았다. 두 실행의 실패·시간·원본을 별도 audit/TensorBoard에 보존한다.

v23은 초기 coarse 접근의 RGB 처리 중 발생하는 명령 공백을 줄이는 후보다. 마지막 유효한 이동만 원본0.6초 TTL·개별0.25초 lease 안에서 이어 주며 만료 후 재개, 권한 상실, 정지/무효 판단, fine 단계에서는 연장을 금지한다. 기존 coarse120회·방향/측면 정렬 기준을 유지한다. protocol의 근거 검토에 따라 총 native wall1800초 안에서 최대2회 전체 실행(첫 성공 시 동일 소스 반복)을 수행한다. 아직 추가 속도 개선이나 새 완주를 주장하지 않는다.

v23 (`7b30aee`, bundle `abc0d78e520fa5994003c36f1e9193352c08fabcbf75fdf99fc5ba5c595d0745`)은 초기 접근59회·갱신32건 후 최종 미세 정렬 및 파지를 통과했다. 상자 갱신21건과 공동 carry도 실행됐으나135.889초에 상자의 TOP RGB 추적 실패로 중단했다. 물리 시계0.99472배속, 평가용 동시 적재 이동5.30 SIM초였다. 명령 시한·권한 감사는 통과했고 로봇/장애물/적재 화물 바닥 접촉·weld는0이지만, 미도착·미방출이므로 물리·프로토콜 실패다. fine 정렬은74.43 SIM초로 기준51–54초보다 길었다. 새 전체 임무 속도 개선은 아직 입증하지 못했으며 audit-v23-full.json 및 TensorBoard 실패 스냅샷에 보존한다.

v24는 v23 실패 TOP 영상의 조각난 색상 후보를 기존 다음 saturation 단계에서도 확인한다. 유일 후보만 기존8px·면적·모양 조건으로 채택하고, 모호하면 기존 optical flow/HOLD를 유지한다. 원본166→167 재생은 이전 코드에서 오류, 수정 후 saturation105의 유일 후보 검출로 확인했다. v23-box-tracking-diagnosis.json에 원본 해시와 결과를 남겼고 관련90개 회귀를 통과했다. 새 물리 완주·속도는 아직 미검증이다.

v24 (`67c537f`, bundle `d1dc432c4bcb176fd5c73cfa65c555c8ba856070b067e4ad8b37ec637c2a6173`)는220.348초에 두 화물 도착·방출·프로토콜 종료에 성공했다. motion0.9940배속, 평가용 동시 적재 이동10.60 SIM초, coarse/solo 갱신37/43건 감사 통과, contact/weld0이다. 그러나 기준196.104–200.929초보다 느리며 속도 개선이 아니다. fine70.45 SIM초와 상자 자원 대기12.54 SIM초가 시간에 포함된다. audit-v24-full.json 및 별도 TensorBoard 원본을 보존하고 동일 후보 반복 대신 명령 공백을 별도로 검토한다.

v25는 먼 fine lateral/forward 이동의 다음 영상 대기에만 총0.05초 이내 추가 연장을 허용하는 후보이다. 기존 .6초 영상 TTL·.25초 개별 lease 및 포트/권한/단계 확인을 유지하고 근접·yaw·정지 확인·final/dock은 제외한다. 기존 pose 모델의 영상 추정 오차와 허용대역을 사용하며 실제 위치를 입력하지 않는다. 새 물리 완주와 시간은 별도 검증한다.

v25 (`dc64f335`) 두 실행은194.183/206.512초에 물리·프로토콜 완주했다. 평균200.348초로 기준198.516초보다0.92% 느려 전체 속도 개선을 입증하지 못했다. fine50.79/57.55 SIM초, solo 자원 대기9.859/12.768 SIM초이며, 접촉·weld 및 명령 권한/시한 감사 오류는0이다. `comparison-v25.json`과 두 audit에 원본 해시·조건을 보존한다. fine 영수증의 실제 필드 `axis`를 읽도록 감사기를 교정하고 정상/위반 회귀2개를 추가했으며, 실행 제어 변경은 아니다.

v26 후보는 기존 개방 지도 독립 경로의 먼 구간 속도 상한을 빔에도 적용한다. 전진.12·측면.10·후진.05, 근접 비례감속과 최종.08, 동기화·공동 하역 독점은 유지한다. 상자 대기의 원인이 된 빔 경로를 단축할 수 있는지 최대2회350초 예산으로 검증하며, 첫 실행 성공·196.104초 미만·불변 조건 감사 통과 때만 반복한다. 관찰 창 해상도 변경은 이 비교에 포함하지 않는다.
