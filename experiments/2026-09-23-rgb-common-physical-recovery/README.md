# 공통 RGB 실행기의 물리 완주 복구 (2026-09-23)

> 2026-09-24 main 반영 범위: 이 기록과 v1~v8 번들 JSON만 은퇴 보존으로 병합했다. 실행 코드는 main의 dispatch 번들 계열과 충돌해 병합하지 않았으며 PR #120 브랜치 `codex/rgb-common-physical-recovery`와 아래 실행 SHA에 남아 있다. main 코드로 이 결과를 재현하지 않는다.

## 판정 범위

`dispatch_open` seed 11에서 기존 성공 실행기와 다른 공통 RGB 실행기를 검사했다. 매 후보마다 상자 단독 운반과 빔 공동 운반을 **각각 한 번** 재생했다. 이는 한 물체씩 분리한 개발 진단이며, 전체 임무의 두 물체 운반이나 통신 방식 비교가 아니다. 두 결과를 합쳐 전체 임무 성공으로 세지 않는다. 제어 입력은 자기 RGB·공용 TOP RGB·자기 명령 이력·정적 지도로 제한했고, 평가 좌표·접촉·성공 판정은 종료 후 별도로 읽었다. weld는 OFF, 외부 모델 호출·통신 메시지·추가 학습은 0이다.

| 후보 / 실행 SHA | 단독 상자 대상 | 공동 빔 대상 | 확인된 경계 |
|---|---|---|---|
| v1 `bf476e2d1aeee8fa72850329f8884676426bef19` | 실패, SIM 179.95초·2,162명령 | 실패, SIM 27.05초·418명령 | 단독 시간 초과, 공동 바퀴 방향 식별 중단 |
| v2 `7c3e4aa6188b0d5623d4977afc7af7746093d548` | 실패, SIM 299.95초·3,123명령 | 실패, SIM 27.05초·422명령 | 상자는 슬롯 안에 있지만 쥔 채 정지, 공동 바퀴 재판독 중단 |
| v3 `050f34170c19add1e5e7c1b5ead56c67b77ddaf3` | **대상 물리 성공**, SIM 279.05초·3,417명령 | 실패, SIM 30.05초·466명령 | 상자 들어 운반·방출·안정화, 공동은 파지 전 자기 바퀴 판독 중단 |
| v4 `2c23383502e597aa62ce643ebe00dc77ee65e8d5` | **대상 물리 성공**, SIM 281.05초·3,442명령 | 실패, SIM 30.05초·470명령 | 상자 성공 재확인, 공동은 다음 겹침 프레임의 자기 바퀴 판독 중단 |
| v5 `c1e63e7761939d678a600ea090304df2ce8323a0` | **대상 물리 성공**, SIM 282.05초·3,422명령 | 실패, SIM 31.05초·478명령 | 상자 성공 재확인, 공동은 양쪽 안쪽 바퀴가 한 영상 성분으로 합쳐져 자기 판독 거절 |
| v6 `c52d36495f8f7807621640c8b5686efefe4bb1ed` | **대상 물리 성공**, SIM 278.05초·3,417명령 | 실패, SIM 46.05초·714명령 | 역할 배정을 영상에 맞췄으나 공동은 coarse 단계 170회 제한으로 파지 전 중단 |
| v7 `13c7996440c726561b379f2a0c080743b06c4163` | **대상 물리 성공**, SIM 280.05초·3,420명령 | 실패, SIM 62.05초·978명령 | 양쪽 coarse 236회 뒤 yaw 단계에서 TOP 방향 등록 거절, 파지 전 중단 |
| v8 `ead4a3a471c2d70ac2ebd73bff200627f99d55c7` | **대상 물리 성공**, SIM 280.05초·3,421명령 | 실패, SIM 64.05초·1,006명령 | yaw 통과 후 양쪽 lateral RGB 모델이 영상 지원 범위 밖으로 거절, 파지 전 중단 |

위 단독 성공 여섯 건은 사후 평가에서 상자 상승·이송, 도착 슬롯 내부, 방출 후 지지·안정화가 모두 참이다. v4 원본 영상의 SIM 72·265·280초, v5의 SIM 72·270·282초, v6의 SIM 72·265·278초, v7의 SIM 280초, v8의 SIM 280초 프레임을 열어 종료 장면도 확인했다. v8 공동 종료 영상도 열어 빔이 바닥에 있는 장면을 확인했다. 이 영상 표본만으로 연속 접촉을 판정하지는 않는다. 사후 평가의 표본 연속 운반 검사에서 바닥 접촉 표본은 0이었으며, 표본 사이 접촉은 주장하지 않는다. 여섯 후보의 `mission_complete`는 모두 거짓이다. 해당 재생에는 다른 물체의 운반 명령이 없기 때문이다.

v5 공동의 r1/r3 181번 TOP 영상에서 안쪽 바퀴가 172px짜리 노란 연결 성분으로 합쳐졌다. 직전 수용 영상의 두 자기 바퀴 영역 간격은 9→7→6→4→2px로 좁아졌고, 양쪽이 서로를 향해 계속 옆걸음 명령을 냈다. 추가 조사에서 고정 요청의 r1 아래/r3 위 역할이 자기 움직임으로 확인한 TOP 영상 배치와 정반대임을 확인했다. v6는 r1 위/r3 아래를 명시하고 실제 자기 바퀴·빔 위치로 역할을 검사했다. 기존 역할은 첫 coarse에서 모두 거절되고 새 역할은 수용됐으며, 새 물리 실행의 170회 coarse 판독에서 양쪽 역할이 모두 유지됐다.

v6 공동은 두 로봇이 SIM 45.25에 `bounded RGB phase exhausted`로 종료됐다. r3는 SIM 13.5부터 접근 준비 상태로 기다렸고, r1은 횡 오차가 마지막까지 0.00452로 0.003 기준보다 약 1.1px 커 전진 명령을 한 번도 내지 못했다. 빔의 사후 상승·변위는 0이다. 정렬 임계값이나 바퀴 마스크를 완화한 성공 판정은 없다. 다음 후보는 TOP 오차의 실제 감소가 있는 동안에만 유한한 coarse 예산을 늘리고 정체할 때 중단하는 방식이다.

v7은 TOP 영상 오차가 줄어드는 경우에만 coarse 한도를 170회에서 최대 300회로 연장했다. 실제 공동 재생에서 두 로봇이 각각 236회까지 진행해 yaw 단계로 넘어갔지만, SIM 61.7의 TOP 영상에서 둘 다 `rgb_pose_outside_support`(`heading_registration_unsupported`)로 종료했다. 빔 상승·변위는 0이다. 독립 사후 감사에서 원본 4,502개와 읽기 전용 자산 13개의 해시, actor 요청 410건·영상 820개 및 스킬 입력 1,413건·영상 2,826개의 경계를 확인했다. 평가/정답 유입은 발견되지 않았다. 감사 원본은 로컬 `outputs/rgb-common-physical-recovery-20260923/audit-v7-physical-13c7996.json`에 보존했다.

v7 원본 판독에서 노란 빔 픽셀이 yaw 등록 중심을 바퀴에서 멀리 끌어간 것이 확인됐다. v8은 자기 움직임으로 식별한 역할과 네 바퀴 모서리의 새 TOP 영상을 등록 중심으로 사용한다. 실제 공동 재생에서 양쪽이 coarse 235회와 yaw 8회씩 통과했으나 SIM 63.7의 첫 lateral 판독에서 모두 `rgb_pose_outside_support`로 거절됐다. 역할별 저장 횡방향 모델의 최근접 지원 거리/한도는 r1 40116.50/16.73, r3 575.51/13.76이며, yaw 시작 전 영상도 같은 모델의 지원 범위 밖이었다. 따라서 이번 범위 이탈을 yaw 동작 때문에 생겼다고 단정하지 않는다. 빔 상승·변위는 0이다. 다음 유한 검증은 저장된 TOP 영상 000367·000379와 역할별 lateral 모델만으로 배경 차분·표준화 특징의 이탈 항목을 비교하는 오프라인 판독 1회다. 이 결과 전에는 새 제어 후보를 정하지 않는다. 독립 사후 감사에서 원본 4,542개, 자산 13개, actor 요청 414건·영상 828개와 스킬 입력 1,425건·영상 2,850개의 경계를 확인했고 평가/정답 유입은 발견되지 않았다. 감사 원본은 로컬 `outputs/rgb-common-physical-recovery-20260923/audit-v8-physical-ead4a3a.json`에 보존했다.

## 실행 계약과 보존

모든 실행 소스를 먼저 커밋하고 깨끗한 체크아웃에서 재생했다. 후보별 JSON 실행 번들, 실제 적용 물리·카메라·명령 일정, 모델·지도·환경·원본 해시를 manifest와 결과에 보존했다. v1~v8은 완료된 개발 후보이며, 공동 물리 완주를 달성한 공통 실행기 번들은 아직 없다. 과거 `VisualMacroExecutor` 성공 판정을 공통 `MacroQueue` 실행기에 승계하지 않는다. 각 후보의 정확한 bundle ID·SHA, 소스 SHA, 2회 결과와 비용, 원본 inventory 경로·SHA-256은 [기계 판독 결과](results.json)에 있다. `manifest_sha256`은 내용의 정규화 JSON 해시이며 원본 파일 바이트의 SHA-256은 별도 필드다.

원본 보고서·영상·요청 입력·명령·평가는 이 작업 환경의 `outputs/rgb-common-physical-recovery-20260923/physical-replay-<SHA7>/`에 있다. 이 폴더는 Git에 포함되지 않는 로컬 자료이며 원격 백업이 아니다. inventory는 v1 2,522개, v2 3,952개, v3 3,726개, v4 3,752개, v5 3,760개, v6 4,092개, v7 4,502개, v8 4,542개 파일을 식별한다. v8의 4,542개 원본과 사전 자산 13개의 해시를 독립 재검사했다. 결과 패키지에는 대량 영상·모델·raw를 복사하지 않았다.

관련 자동 검사와 원본 수집 검사는 실행 기록을 검증하지만, 새로운 물리 완주 근거로 대체하지 않는다. v4 전체 CI는 1,848 passed·8 skipped·205 subtests passed, v5는 1,857 passed·8 skipped·205 subtests passed, v6는 1,859 passed·8 skipped·205 subtests passed, v7은 1,860 passed·8 skipped·205 subtests passed, v8 물리 재생 직전에는 1,861 passed·8 skipped·205 subtests passed였다. 물리 재생을 마치고 최신 `origin/main`을 합친 코드에서는 1,909 passed·8 skipped·205 subtests passed이며 번들 소스 해시도 일치한다. v8 두 재생의 프로세스는 정상 종료·자식 정리됐고, 실제 적용 설정은 `local_contact_fine`·weld OFF였다. 관측 경계 감사에서 위반은 없고 외부 모델 호출·토큰·통신 메시지는 0건이다. 일반 `model_calls` 값은 미측정이며 외부 호출 0과 구분한다. 처음 v7 관리 실행은 시스템 Python 3.14로 시작해 물리 계산 전에 환경 검사에서 차단됐다. 해당 관리 기록을 보존하고 기존 Python 3.12 시뮬레이션 환경으로 재실행했다.

TensorBoard의 `0923-RGB물리-1`부터 `0923-RGB물리-8`까지 후보별 단독·공동 결과와 측정값·원본 영상을 각각 별도 스냅샷으로 등록했다. v8 이벤트·실제 화면의 SIM 280.05/64.05초, 명령 3,421/1,006건, 외부 호출 0건을 원본과 대조했고 원본 영상 주소의 범위 재생도 확인했다. `evaluation/reported_success=0`은 **전체 임무** 판정이다. 단독 상자 대상 성공은 본 기록의 사후 물체 판정으로 읽어야 한다. [TensorBoard 화면](http://127.0.0.1:6006/)은 이 Mac의 로컬 서비스가 실행 중일 때만 열린다.
